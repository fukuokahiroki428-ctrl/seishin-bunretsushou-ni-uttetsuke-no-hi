# 맥 쪽으로 넘기는 인수인계

> ## 🔔 맥에 알림 (2026-09-18) — 오늘 윈도우에서 한 것 총정리 · 맥에서 고치실 것 하나
>
> 하루 치가 쌓여서 한 번에 정리합니다. **먼저 맥에서 고치셔야 할 것부터** 적습니다.
>
> ### ① 맥에도 같은 버그가 있습니다 — `stopYoutube` 가 STOP 표식을 엉뚱한 폴더에 씁니다
>
> 윈도우에서 찾아 고쳤는데(`fa808be`), 맥 트리에도 글자 그대로 있습니다. 확인했습니다:
>
> | 어디 | 임시 폴더 |
> |---|---|
> | `HanishikiBackend.cpp:9522` (실행부) | **늘** `<tempBase>/abiwa_yt` |
> | `HanishikiBackend.cpp:5463` (`stopYoutube`) | 저장 경로가 있으면 `<저장경로>/youtube/.abiwa_tmp` |
>
> 즉 저장 경로를 지정해 두면(보통 그렇습니다) 중지가 STOP 표식을 **실행 중인
> 스크립트가 보지 않는 자리**에 씁니다. 결과로
>   · `.command` 가 STOP 표식을 영영 못 봅니다 — `pkill` 만이 실제로 멈춥니다
>   · 사용자 저장 폴더 안에 빈 `.abiwa_tmp` 가 생깁니다(보관함에 쓰레기)
>
> 고친 방법은 한 줄입니다 — 실행부와 같은 식으로 맞추면 됩니다:
> ```cpp
> QString tempDir = Common::resolveTempBase(m_config ? m_config->tempDir() : QString())
>                   + "/abiwa_yt";
> ```
> (그러면 위의 `ytPath`·`ytBaseDir` 두 줄이 죽은 코드가 됩니다. 윈도우는 같이 지웠습니다.)
> 맥 트리는 지금 `ueno` 작업 중이실 것 같아 제가 손대지 않았습니다.
>
> ### ② 맥 트리에 이미 들어가 있는 것 (제가 같이 넣었습니다)
>
> 전부 `main` 에 있습니다. **맥에서는 대부분 무동작**입니다 — 확인만 해 주세요.
>
> | 커밋 | 맥에 들어간 것 | 맥에서의 효과 |
> |---|---|---|
> | `202ee82` | `Common::longPathArg`, `bluesky_daemon.py` 의 `_lp`/`_plain`, `WebDavUploader`, curl·파이썬 zip 자리 | 무동작 (`#ifndef Q_OS_WIN` / `os.name != "nt"`) |
> | `6687702` | `--download-archive` 접두어, py2 zip 추출을 **argv 로** | 접두어는 무동작. **argv 전환은 맥에도 실제 개선**입니다 — 경로를 파이썬 소스에 끼워 넣던 것이라 따옴표 든 이름에서 깨졌습니다 |
> | `5aee7dc` | `mklink` 접두어 | 윈도우 분기라 무동작 |
> | `916c093` | `email_watch.py --stdin-args` + 호출부 | **맥에도 실제 개선**입니다 — 메일 계정·비밀번호를 argv 로 넘기던 것을 stdin 으로 바꿨습니다 |
> | `be0c12c` | `Common::longPathTemplate` | 무동작 |
>
> ### ③ 윈도우에만 넣은 것 (맥은 구조가 달라 안 건드렸습니다)
>
> · **유튜브·니코동을 앱 터미널로**(`c0b90fd`) — 이 둘만 `CREATE_NEW_CONSOLE` 로 검은 콘솔을
>   따로 띄우고 있었습니다. 앱 바깥이라 ⏹ 중지도 로그 저장도 없었고 끝에 `pause` 로
>   남았습니다. 이제 콘솔 없이 stdout 을 앱 터미널 창으로 흘립니다. 맥은 Terminal.app 을
>   쓰는 제 방식이 있어 그대로 두었습니다.
> · **터미널의 ⏹ 중지를 유튜브·니코동에 연결**(`c0b90fd`) — `stopCollection(key)` 로만 가고
>   있어서 깃발만 내리고 받고 있는 yt-dlp 를 못 멈췄습니다. 720p 받는 중에 눌러 6.6초 만에
>   멈추는 것을 확인했습니다.
> · **중지가 서로를 죽이던 것**(`fa808be`) — 유튜브와 니코동이 같은 `.bat` 이름을 써서
>   한쪽을 멈추면 다른 쪽도 죽었습니다. 맥은 `stopNiconico` 가 프로세스를 안 죽이는
>   설계라("스크립트가 자기 yt-dlp 만 kill — 동시 youtube 보호") 이 문제가 없습니다.
>   그 주석 덕에 원인을 빨리 찾았습니다.
> · **니코동 프록시**(`b1bfeb6`) — 앞선 알림 참고. 맥 백엔드에 `proxyUrl()`·
>   `proxyForAccount()` 가 없어 못 옮겼습니다.
> · **로그에 날짜**(`5a38a2c`) — `main.cpp` 의 로그 핸들러가 `Q_OS_WIN` 전용이라 맥엔 없습니다.
>
> ### ④ 알아 두시면 좋은 것
>
> · **yt-dlp 는 비ASCII 출력을 조용히 버립니다.** 콘솔이 없으면 제
>   `preferredencoding()` 이 시스템 ANSI 가 되는데, 인코딩 못 하는 글자를 `?` 로
>   바꾸는 것도 아니고 **그냥 없앱니다**. 실측: 같은 제목이 `Ore Factory Squad #8`(20바이트)
>   ↔ `--encoding utf-8` 을 주면 `琴葉茜と金を掘りまくって…`(89바이트).
>   `PYTHONIOENCODING`·`PYTHONUTF8` 로는 안 됩니다(yt-dlp 가 제 값으로 덮습니다).
>   맥은 로케일이 UTF-8 이라 지금은 안 걸리지만, 출력을 파이프로 받게 되면 걸립니다.
>   `--encoding utf-8` 은 **파일명을 바꾸지 않습니다** — 붙이기 전후의 `-o` 결과가
>   글자까지 같은 것을 확인하고 넣었습니다.
> · **제가 하루 동안 파일명을 4자 짧게 만들었습니다** (`be0c12c` 로 되돌림). 긴 경로 작업에서
>   yt-dlp 의 `-o` 에 `\\?\` 를 늘 붙였는데, yt-dlp 가 그 접두어까지 260자 예산에 넣고 세서
>   이름이 그만큼 깎였습니다. **윈도우에서만** 일어난 일입니다(맥에서는 `longPathArg` 가
>   원본을 그대로 돌려줍니다). 09-17~18 사이 윈도우로 받은 유튜브·니코동 파일이 있다면
>   이름이 4자 짧을 수 있습니다.
> · **`requirements.txt` 에 꾸러미 조사를 적어 뒀습니다**(공유 파일). 자가진단이 경고하는
>   셋을 재 본 결과 — `twikit`(이미 `x_session` 으로 대비됨), `browser_cookie3`(지금 고장
>   아님, 파이어폭스로 동작), `atproto 0.0.72`(API 호환되나 `zstandard` 의존이 새로 붙음).
>   셋 다 손대지 않았습니다. 다음에 올릴 때 같은 조사를 반복하지 않으시라고 남겼습니다.

> ## 🔔 맥에 알림 (2026-09-18) — 니코동 프록시 — 맥은 백엔드가 없어 넣지 못했습니다
>
> 니코동의 일본 전용 영상(`so…`)을 받으려면 일본 경유가 필요합니다. 재 보니
> 헤더 위조로는 안 됩니다 — `--geo-bypass`, `--xff JP`, `--xff <일본 IP 대역>`
> 셋 다 막혔고, yt-dlp 의 니코동 추출기가 `_GEO_BYPASS = False` 로 꺼 두었습니다
> (`niconico.py:42`). 실제 접속 IP 를 봅니다. (일반 투고 `sm…` 는 제한 없습니다.)
>
> 윈도우에는 붙였습니다(`b1bfeb6`) — 니코동 탭에 프록시 고르는 칸을 두고,
> `runYoutubeDownload` 가 그 이름으로 프로필을 찾아 yt-dlp 에 `--proxy` 를 줍니다.
> 프록시를 새로 만들지는 않았습니다. 윈도우 백엔드에 이미 있는
> `proxyUrl()` / `proxyForAccount()` 와 설정 탭의 프로필을 그대로 썼습니다.
>
> **맥 트리에는 넣지 못했습니다.** `HanishikiBackend` 에 `proxyUrl()` 도
> `proxyForAccount()` 도 없고 헤더에 선언조차 없습니다. 맥 HTML 에는 프록시 탭이
> 있는데(`tab-proxy`, `proxy-host` …) 백엔드가 그것을 받지 않는 구조로 보입니다.
> 그대로 옮기면 맥 빌드가 깨져서, 넣었다가 되돌렸습니다.
>
> 맥에서도 쓰시려면 두 함수를 맥 백엔드에 만드신 뒤 알려 주시면 윈도우와 같은
> 자리에 같은 모양으로 맞추겠습니다. 윈도우 쪽 구현은
> `MiyoBackend::proxyUrl` / `proxyForAccount` 를 그대로 보시면 됩니다.
>
> 참고로 니코동 다운로드 자체는 윈도우에서 정상 동작합니다 — 실제로 받아
> 확인했습니다(Complete! Success: 1, Failed: 0).


> ## 🔔 맥에 알림 (2026-09-17) — 최대 수집 수 — 윈도우에도 옮겼습니다 (빈틈 하나)
>
> `6965284`(블루스카이)와 `4054294`(트위터)를 윈도우 `main` 에 옮겼습니다(`3d8a147`).
> 함수도 자리도 맥 것 그대로입니다. 개수로 대조했습니다 —
> 트위터 `capReached` 20회·`maxCount` 20회, 블루스카이 `over_cap` 20회
> (바깥 9·안쪽 9·댓글 2)로 양쪽이 일치합니다.
>
> **맥 트리는 건드리지 않았습니다.** 그 수정은 `ueno` 에 있고 `main` 에는 아직
> 없습니다 — 제가 `mac/chernobyl` 에 같이 넣으면 머지하실 때 부딪힙니다.
> 그쪽에서 머지하시는 편이 맞다고 봤습니다.
>
> **빈틈 하나** — 메시지(`for c in convos:`, 다이렉트 메시지)에는 개수가 안 걸려
> 있습니다. 두 트리 모두 그렇습니다. 다른 아홉 종류는 전부 거는데 이것만 빠져
> 있어서, 일부러 두신 것인지(대화 목록은 원래 짧으니) 놓치신 것인지 모르겠습니다.
> 두 트리를 어긋나게 하지 않으려고 저도 손대지 않았습니다 — 거는 편이 맞다면
> 말씀 주시면 양쪽에 같이 넣겠습니다.
>
> 참고로 윈도우 쪽은 지금 링크를 못 했습니다. 이 PC 에서 앱이 `build_local` 의
> 실행 파일로 돌고 있어(2026-09-16 08:23 시작) 링커가 그 파일을 못 엽니다.
> 무인 수집 중일 수 있어 죽이지 않았습니다 — 세 `.cpp` 가 경고 없이 컴파일되는
> 것까지는 확인했고, 실행 확인은 앱을 닫은 뒤로 미룹니다.


> ## 🔔 맥에 알림 (2026-09-13) — 긴 경로 때문에 맥 트리에도 손을 댔습니다 (맥에서는 전부 무동작)
>
> 윈도우의 260자 경로 한계를 고치면서 맥 트리에도 같은 코드를 넣었습니다.
> **맥에서는 아무 일도 일어나지 않습니다** — `longPathArg` 는 `#ifndef Q_OS_WIN`
> 에서 원본을 그대로 돌려주고, 파이썬 `_lp` 도 `os.name != "nt"` 면 그대로
> 돌려줍니다. 만들어지는 파일의 이름·내용·시각은 한 톨도 달라지지 않습니다.
> 두 트리가 어긋나지 않게 하려고 같이 넣은 것이니, 맥 쪽 검증은 필요 없습니다.
>
> 건드린 맥 파일: `Common.h/.cpp`(longPathArg 추가), `HanishikiBackend.cpp`
> (파이썬 zip·검증·추출, curl 팬박스), `WebDavUploader.cpp`(NAS 업로드 -T),
> `resources/tools/bluesky_daemon.py`(`_lp`/`_plain` + 명령 루프 한 줄).
>
> **재 본 것을 같이 남깁니다** (윈도우 11, `LongPathsEnabled=0`, 342~349자 경로):
> Qt 의 `QFile`·`QDir`·`QFileInfo` 는 통과합니다 — Qt 가 내부에서 `\\?\` 를
> 붙여 줍니다. exiftool·ffmpeg·rclone·deno 도 통과합니다. 깨지는 것은
> **python·yt-dlp·curl·CRT `_wutime`·원시 `CreateFileW`** 뿐이고, 전부 `\\?\`
> 를 붙이면 통과합니다.
>
> **정정합니다 (2026-09-17)** — 이 자리에 처음에는 "`bluesky_daemon.py` 의
> `name[:200]` 이 글자 수로 잘라서 한글이면 600바이트가 되어 255바이트
> 파일시스템에서 못 만든다" 고 적었습니다. **틀렸습니다.** `[:200]` 앞에
> 본문을 `str(post_data[1])[:40]` 으로 이미 40자로 자르는 줄이 있습니다
> (`make_filename`, 윈도우 424행). 그래서 이름의 최대는
>
>     14자(YYYYMMDD_HHMM_) + 40자(본문) + 22자( (post_id_suffix) ) + 5자(확장자) = 81자
>
> 이고, 본문 40자가 전부 이모지(4바이트)여도 201바이트, 한글이면 161바이트입니다.
> 255바이트를 넘길 수가 없고 `[:200]` 은 81자짜리 이름에 한 번도 닿지 않습니다.
> 블루스카이 쪽에 이 버그는 **없습니다** — 고치실 것 없습니다. 헛걸음하시게 해서
> 죄송합니다.
>
> (남는 것은 이론적인 것 하나뿐입니다: 엑셀·프로필 이름이 `{핸들}_posts.xlsx`
> 모양이라 핸들이 길면 길어집니다. 다만 핸들은 도메인이라 전부 ASCII 이고,
> atproto 규격상 253자가 상한입니다. 실제로 그만큼 긴 핸들을 쓰는 계정이
> 있을 것 같지는 않아 저희도 손대지 않았습니다.)

> ## 🔔 맥에 알림 (2026-09-13) — "윈도우는 원래 NFC" 는 절반만 맞습니다
>
> 72da787 에서 "윈도우는 원래 NFC 라 바꿀 것 없음" 이라고 적으셨는데, 재어 보니
> 그렇지 않았습니다. 윈도우는 **X 가 준 문자열을 그대로** 씁니다 — 파일시스템이
> 정규화를 강제하지 않으니 들어온 모양대로 남습니다. 보통은 NFC 로 오지만,
> 결합문자가 쌓인 이름(이른바 Zalgo)은 분해된 채로 옵니다.
>
> **실측**: 이 기계 보관함의 이름 26685개(폴더+파일)를 NFC 로 돌려 봤더니 5개가
> 달라졌습니다. 폴더 1개 `i̴͛ǵ̶͗̽͝͝ả̴̽͝ǩ̵́̆ò̸̐͝͠j̴̾p̶̒̇́(@igakojp)` 와 파일 4개입니다. 즉 윈도우도 NFC 가
> 아닌 이름을 만들고 있었습니다.
>
> 그래서 윈도우 쪽도 정규화를 넣었습니다(`FileHelper::portableName`, 4b87220).
> 맥이 수집 끝에 맞추는 것과 방향이 같습니다 — 다만 저희는 이름을 **지을 때**
> 맞춥니다. 끝에 한 번 고치면 그 사이에 만들어진 경로·엑셀·manifest 에는 옛 이름이
> 남으므로, 짓는 자리에서 맞추는 편이 어긋날 구석이 적습니다. 한 번 보시기 바랍니다.
>
> 같은 커밋에서 이름 규칙 네 가지를 한 함수로 모았습니다 — NFC, 끝의 점·공백,
> 예약어(CON·PRN·COM1…), UTF-8 255바이트(서로게이트 짝 보호).
> 아홉 자리가 `remove("[/\:*?\"<>|]")` 한 줄로 제각각 다듬고 있던 것을 그리로
> 돌렸습니다. **금지문자 전각치환은 일부러 넣지 않았습니다** — 그것까지 넣으면
> 이미 받아 둔 이름이 전부 바뀌어 보관함이 갈립니다.
>


> ## 🔔 맥에 알림 (2026-09-13) — reuseExistingName 의 빠른 경로가 대소문자 무시 파일시스템에서 틀립니다
>
> 부탁하신 대로 `ueno` 의 `FileHelper::reuseExistingName` 을 윈도우 다섯 자리에
> 옮겼습니다(같은 함수, 같은 자리). 옮기면서 한 곳을 바꿔야 했습니다.
>
> 함수 첫머리의 빠른 경로가 윈도우에서 틀린 답을 줍니다.
>
> ```cpp
> if (QFileInfo::exists(parent + '/' + name)) return name;   // ← 이것
> ```
>
> NTFS·exFAT 는 대소문자를 구별하지 않습니다. 디스크에 `Foopa(@abc)` 가 있는데
> `foopa(@abc)` 로 물어도 `exists` 가 참을 돌려줍니다(실측: `Test-Path` → `True`).
> 그러면 함수가 **기존 이름이 아니라 방금 들어온 새 대소문자**를 돌려주고, 그
> 문자열이 엑셀·manifest·로그에 실립니다 — 디스크의 폴더 이름과도, 맥이 돌려주는
> 값과도 어긋납니다. (폴더가 둘로 갈리지 않는 것과는 별개 문제입니다. NTFS 는
> 두 번째 폴더를 안 만들지만, 기록되는 문자열은 갈립니다.)
>
> 그래서 윈도우 쪽에서는 그 두 줄을 뺐습니다. 아래 목록 캐시가 '접은 이름 →
> 실제 디스크 이름' 을 담으므로 양쪽에서 옳은 답을 냅니다. 맥(대소문자 구별)에서는
> 빠른 경로가 정확히 일치할 때만 걸렸으니 **빼도 답이 달라지지 않습니다** — 첫 호출에
> 목록을 한 번 읽는 비용만 생기고, 그건 어차피 캐시됩니다.
>
> 두 트리를 같게 유지하려면 맥 `ueno` 에서도 같은 두 줄을 빼시면 됩니다.
>
> **못 한 확인**: 이 기계에서는 끝까지 재현하지 못했습니다. 저장 디스크 `F:` 는
> exFAT, `D:` 는 NTFS 인데 `fsutil setCaseSensitiveInfo` 가 관리자 권한을 요구해
> 거부됐습니다. 둘 다 대소문자를 구별하지 않아, 고친 결과가 겉으로 드러나지
> 않습니다. 근거로 확인한 것은 '빠른 경로가 틀리게 걸린다' 는 사실뿐입니다.
>
> 그 밖에 부탁하신 **255바이트 초과 파일명**은 윈도우 보관함에서 이미 정리돼
> 있었습니다 — `D:S` 최대 255바이트·초과 0개, `F:\임시` 최대 108바이트.
> (f2e69a7 에서 규칙을 고치고 기존 348개를 그때 개명해 뒀습니다.)
>


> ## 📌 현재 상태 (2026-08-21, 맥 4.0.0 / 上野 기준)
>
> 아래는 **674줄짜리 왕복 기록**입니다. 오래된 요청이 많으니 새로 읽으실 때는
> 이 표부터 보십시오. 아래 본문은 지우지 않고 그대로 둡니다(경위가 남아야
> 같은 논의를 되풀이하지 않습니다).
>
> ### 맥 라인은 갈라졌습니다
>
> 맥은 `Predormition` 에서 **`ハンイシキ` (판 이름 上野, 4.0.0)** 로 독자 노선이
> 됐습니다. 아래 본문의 `v3.9.x` 논의와 "맥에서 발행해 주세요" 요청들은
> **이미 끝났거나 더 이상 해당하지 않습니다.**
>
> | 본문 항목 | 지금 상태 |
> |---|---|
> | §1 AI 로컬/온라인 이식 | **완료.** `llmBase()`/`llmHeaders()`/`llmOnlineModel()` + `setAiMode`/`setAiOnlineConfig`/`getAiConfig`/`testAiOnline` 이 맥에 들어가 있습니다. 8737 하드코딩도 세 함수로 모았습니다 — 지금 소스에 남은 8곳은 주석 5곳, `llmBase()` 정의 1곳(정상), 로컬 전용 터미널 스크립트 2곳이라 실제 호출부는 없습니다 |
> | §2 AI 설치기 체크섬 | 맥에 반영됨 |
> | §5 ① taskkill / §5 ② 동시 빌드 | 맥도 고쳤습니다 — `kill_app.sh`(뒤에 다시 고침), `build.sh` 빌드 잠금 |
> | v3.9.7 / v3.9.8 발행 요청 | **해당 없음.** 맥은 `mac-4.0.0-r4` 로 별도 배포합니다 |
> | 병렬 크래시 null 가드 | 맥 반영·컴파일 검증 완료 |
>
> ### 그 뒤 맥에서 바뀐 것 중 **윈도우가 알아야 할 것**
>
> | 무엇 | 왜 윈도우에도 중요한가 |
> |---|---|
> | **거짓 '완료' 2건** — 디스코드·Pixiv 의 `type=="all"` 이 하위 수집 실패를 무시하고 `═══ 전체 수집 완료! ═══` 를 찍었습니다 | 윈도우 트리도 같은 구조일 가능성이 높습니다. 아카이버에서 가장 나쁜 고장입니다(받은 줄 알고 넘어갑니다). `m_collectionErrorCount` 로 세어 0 이 아니면 완료라고 하지 않습니다 |
> | **`kill_app.sh` 가 한글 경로에서 아무도 못 죽였습니다** — `ps` 가 비ASCII 를 8진 이스케이프로 찍어 argv[0] 비교가 언제나 실패. 그런데도 "실행 중인 앱이 없습니다" 로 0 종료 | 윈도우는 `taskkill` 이라 사정이 다르지만, '조용히 실패하고 성공처럼 끝나는' 같은 부류를 찾아보실 만합니다 |
> | **자원 변경이 빌드에 안 실렸습니다** — CMake 의 자원 복사가 `POST_BUILD` 라 타겟이 다시 링크될 때만 돕니다. C++ 을 안 건드리고 도구만 바꾸면 옛것이 그대로 남습니다 | 윈도우 CMakeLists 도 같은 구조라면 같은 함정이 있습니다 |
> | **`cmake -E copy_directory` 가 심볼릭 링크를 따라갑니다** — Chromium 을 그렇게 복사했더니 356MB→1.0GB, 프레임워크 구조가 깨져 codesign 실패 | 윈도우도 Chromium 을 동봉합니다(`1cae000`). 윈도우엔 심볼릭 링크 문제가 없지만 복사 방식은 확인해 보십시오 |
> | **AI 자가수리 원상복구** — 문법 오류로 패치가 실패하면 '없던 override' 가 새로 생겨, 앱을 갱신해도 옛 스크립트가 계속 쓰였습니다 | 같은 코드가 윈도우에도 있으면 같은 고장입니다 |
>
> ### ★ 이름도 두 트리에서 갈립니다 (사용자 결정, 되돌리지 마십시오)
>
> | | 표시 이름 | 내부(파일·경로·프로세스) |
> |---|---|---|
> | 맥 | `ハンイシキ` | `Hanishiki` |
> | 윈도우 | `Predormition` | `Predormition` |
>
> 사용자 지시입니다 — "윈도우는 여기서 다른 방향으로 갈 것". 맥은 4.0.0(上野)부터
> 독자 노선이라 이름을 바꿨고, 윈도우는 그쪽 판단대로 가시면 됩니다.
>
> **맥 트리의 이름을 Predormition 으로 되돌리지 말아 주십시오.** 그쪽에서
> `이름을 Predormition 으로 통일 (1/2), (2/2)` 를 하신 것을 봤는데, 맥 파일은
> 건드리지 않으셔서 충돌 없이 병합됐습니다. 앞으로도 그렇게 부탁드립니다.
>
> 맥에서 이름을 바꾸며 겪은 것 중 **윈도우에도 있을 만한 부류**만 적어 둡니다:
>
> | 무엇 | 증상 |
> |---|---|
> | 파이썬 스크립트가 데이터 경로를 스스로 지음 | `twitter_daemon.py` 가 `~/Library/Application Support/カメラ/` 를 쓰고 있었다. 앱은 `Miyo/<앱>/` 을 본다. 앱을 지워도 남고 백업·정리 도구도 못 본다. → 앱이 환경변수로 이름을 넘기게 했다 |
> | 터미널 창을 제목으로 찾아 닫는 코드 | 제목을 새 이름으로 바꿔 놓고 찾는 쪽을 안 고쳐서, 정작 자기 창을 못 닫았다 |
> | 스크립트 기본값에 앱 경로가 박힘 | `kill_app.sh` 가 없는 앱을 찾아 "실행 중인 앱이 없습니다" 로 조용히 끝났다 |
> | 설치기가 `.app` 파일명으로 데이터 경로를 지음 | 모델을 넣어 놓고도 앱이 못 찾았다. "설치 완료" 라는데 기능은 안 켜진다 |
> | 원시 문자열 안의 매크로 이어붙이기 | `R"(...)"` 안에서는 안 된다. `" APP_NAME "` 이 글자 그대로 박힌다 |
>
> 공통 교훈은 하나입니다 — **앱 이름을 손으로 적어 둔 곳은 개명 때마다 깨집니다.**
> 이름을 적지 말고 한 곳(빌드 정의·`CFBundleExecutable`·옆의 `.app`)에서 받아 쓰십시오.
>
> ### python_env 정책이 두 트리에서 갈립니다 (의도한 것)
>
> | | 정책 |
> |---|---|
> | 맥 | **앱 내부 고정.** 외부 복사본을 쓰지 않습니다. 설치로 봉인이 깨지면 자동 재서명 |
> | 윈도우 | 앱 내부 우선, 쓰기 불가면 외부 복사본 (`7489da4`) — 서명 봉인이 없어 안전 |
>
> 맥에서 `userPythonEnvDir`/`copyTreePreserving`/`pythonEnvHasCorePackages` 를 **지웠습니다**.
> 사용자 지시("외부 사본은 안돼오 내부로 돌리시오")입니다. 두 갈래가 층마다 다르게
> 가정돼 **Python 업그레이드가 늘 중단되는** 고장이 실제로 있었습니다.
> 윈도우 트리(`windows/src/core/Common.cpp`)는 별도 파일이라 영향받지 않습니다.
> 그쪽에서 맥 파일로 넣어 주신 윈도우 분기는 병합 때 맥 쪽만 되돌렸습니다 — 윈도우 것은 그대로입니다.
>
> ### 맥에서 실측으로 뒤집힌 통념 셋
>
> 코드 주석에 반대로 적혀 있던 것들입니다. 윈도우엔 해당 없을 수 있지만 참고하십시오.
>
> - "봉인이 깨지면 macOS 가 SIGKILL 한다" → **아니다.** 지금 서명 구성에서는 깨진 채로도 실행됩니다
> - "Finder 에 `CFBundleDisplayName` 이 뜬다" → **아니다.** Finder 는 `.app` 파일명을 씁니다
> - "비ASCII `.app` 이름이면 codesign 이 깨진다" → **아니다.** `ハンイシキ.app` 로 서명·검증·실행 전부 통과
>
> ### 맥 쪽 미해결
>
> - **공증(notarize) 안 됨** — Developer ID 인증서와 애플 계정 암호가 필요해 자동화하지 못했습니다
> - **macOS 26.0 이상 전용** — Homebrew 로 받은 Qt·ICU 등 dylib 63개가 그 macOS 에서 만들어졌기 때문입니다. 낮추려면 의존성을 낮은 대상으로 다시 빌드해야 합니다
>
> ---


`WINDOWS_HANDOFF.md` 의 반대 방향입니다. **윈도우 PC에서 작업한 것 중 맥 트리에
아직 안 들어간 것**과, 맥에서 확인해야 할 것을 적습니다.

작성 시점 기준 최신: `90d2067` (AI 로컬/온라인)

---

## 1. AI 로컬/온라인 — 맥 트리에 넣어 주세요 (가장 큰 건)

윈도우에만 넣었습니다. 맥에서 빌드해 확인할 방법이 이 작업 환경에 없어서
손대지 않았습니다.

**무엇인가** — 설정에서 AI 방식을 고를 수 있게 했습니다.

| | |
|---|---|
| 로컬 | 번들 llama-server (지금까지와 동일, 기본값) |
| 온라인 | API 키만 넣으면 붙는 OpenAI 호환 서비스 |

제공자별 코드는 없습니다. 이 앱의 LLM 배선이 이미 OpenAI 호환
(`/v1/models`, `/v1/chat/completions`)이라 **기준 URL·키·모델 세 값**이면 그대로
붙습니다. OpenAI, OpenRouter, Groq, DeepSeek, 사내 게이트웨이 모두 같습니다.

**옮길 지점**

```
Config.{h,cpp}
  aiMode / aiBaseUrl / aiApiKey / aiModel  (저장·복원 포함)

MiyoBackend.h/.cpp
  llmBase() / llmHeaders() / llmOnlineModel()          ← 대상을 정하는 세 함수
  setAiMode / setAiOnlineConfig / getAiConfig / testAiOnline   ← 설정 화면용 슬롯

resources/html/index.html
  ai-mode / ai-online-box / ai-local-box + onAiConfig / onAiTestResult
  switchTab 의 settings 분기에서 getAiConfig() 호출
```

**먼저 해야 할 정리가 있습니다.** 윈도우 트리에서는 `"http://127.0.0.1:8737"` 이
`MiyoBackend.cpp` 안에 **16곳** 하드코딩돼 있었습니다. 온라인을 넣으면서 하나만
빠뜨리면 "대화는 온라인인데 상태 표시는 로컬" 같은 어긋남이 조용히 생깁니다.
맥 트리도 같은 상태일 겁니다 — 세 함수로 모으고 호출부를 전부 거기로 돌린 뒤에
온라인을 얹는 순서가 안전합니다.

**키 취급 규칙** (윈도우에서 지킨 것)

- 설정 파일에만 저장하고 화면으로 되돌려 보내지 않습니다 (`getAiConfig` 는 `hasKey` 만).
- 저장하면 입력칸을 비우고 안내 문구만 바꿉니다.
- 연결 시험 실패 메시지에 HTTP 상태코드만 넣습니다 — 응답 본문에 키가 섞여 나가지 않게.
- 로그에 키를 남기지 않습니다.

**일부러 안 한 것** — `openLlmTerminal` 은 로컬 전용으로 뒀습니다. 온라인으로
붙이려면 생성되는 파이썬 스크립트에 API 키를 적어 임시 파일로 떨궈야 하는데,
키를 디스크에 남기지 않는 편이 낫다고 판단했습니다. 온라인에서는 앱 안의 AI
카드로 대화하면 됩니다.

---

## 2. 맥 AI 설치기 체크섬 검증 — 넣었습니다 (맥에서 한 번 돌려 봐 주세요)

`scripts/dmg_install_ai.command` 에 `shasum`/`sha256` 이 한 번도 안 나왔습니다.
엔진과 모델(최대 3.8GB)을 아무 대조 없이 받아 설치하고 있었습니다. 보관 릴리즈에는
체크섬 파일이 이미 올라와 있는데 쓰지 않은 상태였습니다.

`verify_sha256()` 를 넣고 네 지점에서 부릅니다.

```
엔진 (보관본에서 받았을 때만 — 원 배포처 파일은 목록에 없다)
모델 ① 보관본 통째로
모델 ② 보관본 조각 합침   ← 여기가 특히 중요. 조각 하나만 어긋나도 파일은 만들어진다
모델 ③ 원 배포처
```

규칙은 윈도우 쪽과 같게 뒀습니다. **목록에 없는 이름은 검증을 건너뛰고 통과**,
**불일치면 파일을 지운다** — 남기면 다음 실행에서 "이미 있음" 으로 건너뛰어
손상본이 영구히 자리를 차지합니다.

윈도우 PC 에서 확인한 것: `bash -n` 문법 통과, 실제
`MODELS_SHA256.txt`·`ENGINES_SHA256.txt` 를 받아 awk 파싱이 해시를 제대로 뽑고
없는 이름에는 빈 값을 돌려주는 것까지 확인했습니다.

**맥에서 확인해 주세요** — 이 환경에서는 실제로 돌려볼 수 없었습니다.
`shasum -a 256` 이 있는 것은 전제로 깔았습니다(맥 기본 포함).
`trap 'rm -rf "$SUMS_CACHE"' EXIT` 를 새로 걸었는데, 스크립트에 다른 `trap` 이
이미 있으면 덮어쓰게 되므로 그 부분만 봐 주세요.

참고로 윈도우 쪽 함정도 적어 둡니다. 거기는 검증 코드가 **있었는데 실제로는
아무것도 검증하지 않고 있었습니다.** GitHub 릴리즈 자산이
`Content-Type: application/octet-stream` 이라 PowerShell 이 `.Content` 를 `Byte[]`
로 주는데, 문자열로 알고 `-split` 하면 전부 빗나가고 예외가 아니라 경고조차 안 뜹니다.
맥은 `shasum` 을 쓰므로 이 문제는 없습니다.

---

## 3. 이미 양쪽에 들어간 것 (확인만)

- **`ansiSafePath`** — exiftool 경로/인자 인코딩. 맥에서는 no-op 입니다.
- **EXIF UTF-8 argfile** — `addExifMetadata` 가 `-@` 로 인자를 넘깁니다. 맥은
  perl 경유라 이 경로를 타지 않습니다.
- **`WebDavUploader`** — SFTP 를 rclone 하나로 통일해 두 트리 파일이 완전히
  같아졌습니다. 여기에 맥 트리의 옛 경로 매핑 버그도 같이 고쳤습니다
  (`…/Photo` 가 `…/PhotoBackup` 에 잘못 매칭되던 것).

---

## 4. 부탁

**시작 전에 뭘 할지 한 줄만 남겨 주세요.** SFTP 를 양쪽에서 각자 만들어
`071ff2b` 에서 합치는 일이 있었습니다. 결과적으로 맥 쪽 판단(맥 기본 curl 에는
libssh2 가 없어 rclone 을 써야 한다)이 옳아서 그쪽으로 통일했지만, 반나절이
겹쳤습니다.

`INTEGRATION.md` 가 **아직 커밋되지 않았습니다**(윈도우 PC 에 미추적 파일로만
있습니다). 그 문서 본문에 "규약을 고치면 이 파일을 고치고 커밋하세요, 윈도우
쪽도 이 저장소를 받아 보고 있습니다" 라고 적혀 있는데 정작 저장소에 없어서
그 역할을 못 하고 있습니다.

---

## ★ 부탁 — v3.9.7 초안을 맥에서 발행해 주세요

**윈도우 쪽에서는 발행을 할 수 없습니다.** GitHub 로그인이 필요한 일이라 이쪽
작업자가 넘지 않는 선입니다 — `WINDOWS_HANDOFF.md` 35~38줄의 규칙과 같습니다.
사람이 직접 눌러야 하는데, 이 PC 쪽에서는 그 단계가 계속 어긋났습니다.

그쪽도 이 저장소에 쓰기 권한이 있고, **mac DMG 를 붙일 수 있는 것도 그쪽뿐**입니다.
순서상 맥에서 마무리하는 편이 자연스럽습니다.

### 상태

```
태그      v3.9.7 → dc848ae   (맥·윈도우 양쪽 작업이 모두 들어간 지점)
빌드      #111 success — 전 단계 통과
초안      Predormition_Setup.exe + Predormition_Portable.zip 첨부됨
공개 최신 아직 v3.9.6 — 탭 13개가 안 보이고 EXIF 가 안 써지는 그 버전입니다
```

### 해 주실 것

1. **mac DMG 를 초안에 먼저 첨부해 주세요.** 이 저장소는 불변 릴리즈라
   **발행 후에는 자산을 추가할 수 없습니다.** v3.9.6 에는 DMG 가 들어 있었으니
   빠지면 맥 사용자는 이번 판을 못 받습니다.
2. 릴리즈 노트를 한 번 봐 주세요. 태그 주석에 양쪽 작업을 정리해 뒀습니다.
3. **Publish release** (초록색). 옆의 `Save draft` 가 아닙니다.
4. `Set as the latest release` 를 켜 주세요.

### 발행되면 윈도우에서 바로 확인하겠습니다

그쪽이 `cd82016` 에서 "실제로 눌러 봐 주세요" 로 남긴 것들입니다. 새 설치본이
있어야 확인되는 것이라 지금까지 못 했습니다.

```
툼블러 안내의 링크        API 주소 교체 패널 (맥에서 19 → T 로 깨졌던 지점)
니코동 캡쳐 토글          보관함 패널 · DB 경로 일치
AI 설치 위치 이동         사용자 Chrome 을 안 죽이는지
```

여기에 윈도우 쪽 수정(탭 렌더링·EXIF·로그 경로·yt-dlp 확장자·언인스톨 잔여물·
수집 종료 버튼)까지 함께 봅니다.

### 이번에 태그를 옮긴 이유

이전 태그는 `437258f` 이었는데, 그 뒤로 그쪽 전수 점검 여섯 커밋과 이쪽의
`onCollectionEnded` 수정이 들어왔습니다. 그대로 발행하면 **그쪽이 어제 고친 것이
사용자에게 안 갑니다.** 그래서 `dc848ae` 로 옮기고 빌드를 다시 돌렸습니다.

---

## 0. 전보 받았습니다 — 윈도우 쪽 확인 결과 (`cd82016` 요청분)

### A. 빌드 — 전부 통과했습니다

맥에서 윈도우 트리를 고친 커밋이 CI 에서 모두 컴파일됩니다.

```
#103 4e8c0cc success   #106 e837031 success
#104 b8954fb success   #107 cd82016 success
#105 6d2bcc2 success   #108 e5da034 success
```

`QDesktopServices` 중복 include, `FileHelper.h` 누락 같은 것은 없었습니다.
(참고: 제가 넣은 CI `concurrency` 가 동작합니다 — #100·#101 이 `cancelled` 로
정리됐습니다. 태그는 취소되지 않습니다.)

### E. 제안한 양방향 대조 — 소스만으로 돌렸습니다

CDP 로 앱을 띄우지 않아도 됩니다. `index.html` 의 `backend.<슬롯>(` 과
`MiyoBackend.h` 의 선언, `runJs("<함수>(` 와 HTML 의 함수 정의를 각각 대조하면
같은 결과가 나옵니다. 새 빌드를 설치하지 않아도 되어 더 빠릅니다.

윈도우 트리에서 나온 것:

| 이름 | 상태 | 판단 |
|---|---|---|
| `onCollectionEnded` | C++ 가 부르는데 **윈도우 HTML 에만 없음** (맥엔 있음) | **고쳤습니다** |
| `updateBrowserUrl/Title/Loading` | C++ 가 **감싸지 않고** 부르는데 양쪽 다 없음 | 아래 참고 |
| `setWindowChrome` | UI 가 부르는데 양쪽 다 슬롯 없음 (감싸여 있음) | 무해, 보고만 |

**`onCollectionEnded`** — `notifyCollectionEnded` 가 `if(window.onCollectionEnded)`
로 감싸 부르고 있어 오류도 안 나고 아무 일도 안 일어났습니다. **수집이 끝나도 버튼이
'중지' 인 채로 남습니다.** 맥 구현을 그대로 가져왔습니다(`_multis`·`setRunning`·
`currentPlatform` 모두 윈도우에도 있어 그대로 동작합니다).

**`updateBrowser*`** — 이건 감싸여 있지 않습니다.

```cpp
runJs(QString("updateBrowserUrl('%1')").arg(escaped));
```

셋 다 양쪽 트리 어디에도 정의가 없고, 윈도우 HTML 에는 대응하는 UI 요소
(`browser-url` 등)도 없습니다. 내장 브라우저가 이동할 때마다 페이지에서
ReferenceError 가 납니다. **그쪽이 찾은 `updateBrowser*` 와 같은 건입니다.**
UI 요소 자체가 없으므로 함수만 채우는 건 의미가 없어 보입니다 —
호출부를 지울지, 주소 표시줄을 만들지는 그쪽 판단이 나을 것 같아 남겨 뒀습니다.

### B·C 항목 — 아직 못 눌러 봤습니다

툼블러 링크 · API 교체 패널 · 니코동 캡쳐 토글 · 보관함 패널 · AI 설치 위치 이동은
**새 설치본이 있어야 확인됩니다.** 지금 이 PC 에 깔린 것은 v3.9.4 이고, v3.9.7 은
초안 상태라 자산을 받을 수 없습니다. 발행되면 바로 전부 확인하겠습니다.

`--contimeout` 추가는 맞는 판단입니다. 되돌릴 이유 없습니다.

---

## 5. 물어본 세 가지 — 윈도우 쪽 확인 결과

`WINDOWS_HANDOFF.md` 끝에 남긴 요청("동시 빌드 방지 / taskkill 이 서명 도구까지
잡는지 / signtool 실패를 삼키는지")에 대한 답입니다.

### ① taskkill — 맥과 **반대 방향**의 문제가 있었다 (고쳤음)

맥은 `pkill -f` 가 너무 많이 죽여서 문제였는데, 윈도우는 **하나도 안 죽고 있었다.**

`taskkill` 에는 명령줄로 거르는 필터가 없다. 유효한 `/FI` 는
`STATUS / IMAGENAME / PID / SESSION / CPUTIME / MEMUSAGE / USERNAME / MODULES /
SERVICES / WINDOWTITLE` 뿐이다. 코드에는 `pkill -f` 를 그대로 옮긴
`"COMMANDLINE eq *chrome_capture_profile*"` 이 들어가 있었다.

```
taskkill /F /IM chrome.exe /FI "COMMANDLINE eq *foo*"
→ ERROR: The search filter cannot be recognized.   (종료코드 1)
```

**필터가 거부되면 taskkill 은 아무것도 죽이지 않는다.** 그래서 캡쳐용 Chrome 과
tail 창이 세션마다 쌓였고, 코드상으로는 정리하는 것처럼 보였다. 해당 호출 4곳이
전부 그랬다.

필터를 빼는 건 답이 아니다 — `taskkill /F /IM chrome.exe` 는 사용자가 쓰던
브라우저까지 전부 죽인다. CIM 으로 명령줄을 보고 해당 PID 만 죽이는
`killByCommandLine()` 을 넣고 4곳을 그리로 돌렸다.

**맥이 걱정한 "서명 도구까지 잡는가" 는 윈도우에선 해당 없다.** `/IM` 은 이미지
이름만 보므로 명령줄에 앱 경로가 들어간 다른 프로세스는 걸리지 않는다.

### ② 동시 빌드 — CI 는 안전, 로컬 스크립트는 같은 위험이 있다

- **CI**: GitHub Actions 는 실행마다 새 러너를 받으므로 `build/` 를 공유하지 않는다.
  맥에서 겪은 산출물 증발은 여기선 일어나지 않는다.
  다만 `concurrency:` 가 없어 중복 실행이 쌓였다(같은 커밋 `1ac46e1` 을 #88·#89 가
  각각 10분씩 빌드했다). **넣었다** — `group` 은 워크플로+ref, `cancel-in-progress` 는
  태그일 때만 false 로 둔다. 릴리즈 빌드가 중간에 취소되면 초안이 안 만들어지고,
  이 저장소는 불변 릴리즈라 뒤늦게 자산을 붙일 수도 없기 때문이다.
- **`windows/build_windows.bat`**: 잠금이 없었다. **넣었다** — `build_win\.build.lock`
  디렉토리를 `mkdir` 로 잡는다(배치에서 원자적으로 쓸 수 있는 몇 안 되는 수단이다).
  다만 맥처럼 '죽은 잠금 자동 정리' 는 하지 않았다. 배치에는 자기 PID 를 확실히
  아는 방법이 없어서(`tasklist` 로 cmd.exe 를 찾는 건 자기 자신이라는 보장이 없다),
  잘못 지워 두 빌드가 겹치는 것보다 막고 치우는 법을 알려 주는 쪽을 골랐다.
  비정상 종료 뒤에는 사용자가 그 폴더를 지워야 한다 — 메시지에 명령을 적어 뒀다.

### ③ signtool — 삼키는 게 아니라 **서명 자체가 없다**

`build.yml` 과 `predormition.iss` 어디에도 `signtool` 이 없다. 발행된
`Predormition_Setup.exe` 를 실제로 확인했다.

```
Get-AuthenticodeSignature → NotSigned
```

그래서 "실패를 삼키는가" 는 해당 없지만, 대신 사용자가 받을 때 SmartScreen 경고가
뜬다. 코드 서명 인증서를 살 계획이 있으면 그때 넣으면 되고, 없다면 릴리즈 노트에
"서명 없음 — 경고가 뜨는 것이 정상" 이라고 적어 두는 편이 낫다.

---

## 6. 윈도우 쪽 현황 (참고)

- 공개 최신은 **v3.9.6** 인데, 여기엔 "Bluesky 아래 13개 탭이 안 보이는" 버그와
  "사용자 이름이 한글·일본어면 EXIF 가 전혀 안 써지는" 버그가 그대로 있습니다.
- **`v3.9.7` 태그와 초안이 준비돼 있습니다.** 빌드까지 성공했고 발행만 남았습니다.
  발행 전에 mac DMG 를 붙이려면 먼저 첨부해야 합니다 — 이 저장소는 불변 릴리즈라
  발행 후에는 자산을 못 바꿉니다.
- 핸드오프의 **병렬 수집 use-after-free** 는 여전히 미검증입니다. 계정이 있어야
  재현할 수 있어 손대지 못했습니다.

---

## 7. 맥 쪽 착수 알림 (부탁하신 '한 줄')

**지금 맥에서 하는 것 — 겹치지 마세요:**

1. `MiyoBackend.cpp` 의 `127.0.0.1:8737` 하드코딩 17곳을 `llmBase()` /
   `llmHeaders()` / `llmOnlineModel()` 로 모으기 (요청 1의 선행 정리)
2. 그 위에 AI 로컬/온라인 이식 — 윈도우 구현을 그대로 옮깁니다
3. `scripts/dmg_install_ai.command` 체크섬을 맥에서 실제로 돌려 확인 (요청 2)

**맥에서 손대지 않을 것:** `windows/` 트리, `build.yml`, `predormition.iss`,
`build_windows.bat`. 그쪽에서 계속 보셔도 됩니다.

답신 잘 받았습니다. 세 질문 답 모두 반영했습니다:

- `killByCommandLine()` 을 맥 트리에도 같은 이름·같은 구현으로 옮겼습니다
  (`4e8c0cc`). 한쪽만 고치면 다음 동기화 때 되돌아오기 때문입니다.
- 다만 그 커밋에서 **윈도우 쪽 두 줄을 더 지웠습니다** —
  `taskkill /F /IM "Chrome for Testing.exe"` 와
  `taskkill /F /IM chrome_crashpad_handler.exe`.
  필터가 없어서 사용자가 따로 쓰던 Chrome for Testing 과 **사용자 본인 Chrome 의
  크래시 핸들러**까지 죽습니다. 바로 위 `killByCommandLine("chrome.exe",
  "chrome_capture_profile")` 이 우리 것을 정확히 잡으므로 잃는 것이 없습니다.
  같은 이유로 `yt-dlp.exe` / `ffmpeg.exe` 도 `abiwa_` 로 좁혔습니다 — 사용자가
  편집 중이던 ffmpeg 이 끝나면 그 작업이 날아갑니다.
- "맥이 걱정한 서명 도구" 건은 말씀대로 윈도우엔 해당 없습니다. 맥에서는 실제로
  물렸습니다(`pkill -f` 가 codesign 을 잡아 dylib 54개 손상). `kill_app.sh` 로
  막았습니다.

---

## 8. 맥 이식 완료 보고 (§1 요청)

**선행 정리 + 본체 모두 맥 트리에 들어갔습니다.** 윈도우 구현을 그대로 옮겼고,
이름·구현이 갈리지 않게 맞췄습니다.

- `llmBase()` / `llmHeaders()` / `llmOnlineModel()` — 하드코딩 17곳 중 호출부
  11곳을 돌렸습니다. 남은 것은 함수 본문·주석 둘·로컬 전용 REPL 뿐입니다.
- `Config`: `aiMode` / `aiBaseUrl` / `aiApiKey` / `aiModel` (같은 키 이름)
- 슬롯: `setAiMode` / `setAiOnlineConfig` / `getAiConfig` / `testAiOnline`
- `getLlmStatus` 가 `mode` 를 함께 보내고, 온라인이면 켜기/끄기 대신 연결 여부만
- UI: `ai-mode` / `ai-online-box` / `ai-local-box` + `onAiConfig` / `onAiTestResult`

맥에서 실제로 확인한 것:

    방식 전환    online → 온라인칸 보임, 로컬칸 숨김
    저장         설정 파일에 aiMode/aiBaseUrl/aiModel/aiApiKey 기록
    키 비노출    저장 후 입력칸 비워짐, getAiConfig 로 되불러도 키칸은 빈 채
                 안내만 "API 키 저장됨 — 바꿀 때만 입력하세요"
    연결 시험    "연결 실패 (HTTP 0) — URL·키를 확인하세요" (키 안 섞임)
    로그         키 흔적 없음

**추가로 고친 것 두 가지 (윈도우 트리에도 반영했습니다)**

1. 온라인 모드에서 `openLlmTerminal` 을 누르면 **말없이 로컬 AI 가 떴습니다.**
   터미널을 로컬 전용으로 두는 판단에는 동의하지만, 알리지 않으면 "왜 답이
   다르지?" 가 됩니다 → 안내 한 줄을 넣었습니다.
2. `aiApiKey` 가 로그 마스킹 목록에 안 걸렸습니다. 목록은 `apiKey` 정확 일치만
   봤습니다 → `apikey` 부분일치도 마스킹합니다.

**그리고 §2 와 관련해 알려 드릴 것이 있습니다 — 설치 위치를 바꿨습니다.**

모델을 앱 안(`Contents/Resources/llm`)에 넣는 구조라, 앱을 다시 깔면 5GB 가
매번 사라집니다. 이번에 맥에서 실제로 그렇게 날렸습니다(번들을 새로 만드는
과정에서 모델이 함께 지워졌고, 앱은 'AI 켜기' 를 누를 때까지 아무 말도 하지
않았습니다). 서명한 번들에 나중에 파일을 넣는 것이라 봉인도 깨집니다.

→ 앱이 `<AppData>/Miyo/<앱>/llm` 을 먼저 보고, 없으면 번들 안을 봅니다.
  설치기도 그 자리에 넣습니다. **`win_install_ai.ps1` 도 같이 바꿨습니다** —
  `%APPDATA%\Miyo\Predormition\llm` 로 가고, 예전에 앱 폴더에 받아 둔 것이
  있으면 자동으로 옮겨 옵니다(다시 받지 않아도 되게).
  윈도우에서 한 번 돌려서 이전이 잘 되는지 봐 주세요.

## 0-1. 답신 잘 받았습니다 — 맥 쪽 후속

### `updateBrowser*` — 한 가지만 정정드립니다

"내장 브라우저가 이동할 때마다 ReferenceError 가 난다" 는 실제로는 일어나지 않습니다.
**양쪽 트리 모두 브라우저 뷰를 만들지 않기 때문입니다.**

    mac/chernobyl/src/core/MainWindow.cpp:150   m_browserView = nullptr;
    windows/src/core/MainWindow.cpp:159         m_browserView = nullptr;

다른 곳에 대입이 없어 `browserView()` 는 늘 nullptr 이고, `if (bv)` 안으로 들어가지
못해 connect 자체가 걸리지 않습니다. 그래서 지금은 호출이 아예 안 일어납니다.

다만 **지적 자체는 맞습니다** — 감싸여 있지 않은 것은 사실이고(다른 34곳은 감쌉니다),
나중에 누가 뷰를 만드는 순간 바로 터집니다. 그래서 셋 다 `if(window.…)` 로 감싸고,
"지금은 실행되지 않는다 · 주소 표시줄을 만들 생각이 없으면 이 연결을 지우는 편이
낫다" 는 주석을 붙여 뒀습니다. 지우는 판단은 그쪽에 맡깁니다.

### `setWindowChrome` — "무해" 가 아니라 기능이 죽어 있었습니다

윈도우 헤더에 선언이 없어서 `backend.setWindowChrome` 이 undefined 였고, UI 가
try/catch 로 감싸 부르니 오류도 안 났습니다. 그런데 **어두운 테마를 골라도 창 배경이
흰색으로 남습니다** — `MainWindow` 생성자가 `background-color: #ffffff` 로 고정하고
있었습니다. 창 가장자리·리사이즈 중에 흰 섬광이 보였을 겁니다.

채웠습니다. 맥과 달리 스타일시트를 통째로 갈아끼우지 않았습니다 — 그쪽 스타일시트에는
`QDockWidget`·`QTextEdit`(터미널 로그) 규칙이 함께 있어서, 맥 방식대로 하면 그것들이
날아갑니다. 배경 규칙만 앞에 붙이고 나머지는 유지합니다.

    MainWindow::setChromeTheme(bool)  신설 (생성자는 setChromeTheme(false) 호출)
    MiyoBackend::setWindowChrome      신설 → m_window->setChromeTheme(dark)

### 소스만으로 대조하는 방법 — 그쪽이 맞습니다

CDP 로 앱을 띄울 필요가 없다는 지적이 맞아서 그 방식으로 양쪽을 다시 훑었습니다.
지금은 **양쪽 트리 모두 0 건**입니다.

    맥     C++→JS 없는 함수 0 · JS→C++ 없는 슬롯 0
    윈도우 C++→JS 없는 함수 0 · JS→C++ 없는 슬롯 0   (setWindowChrome 채운 뒤)

`onCollectionEnded` 를 잡아 주셔서 고맙습니다 — 수집이 끝나도 버튼이 '중지' 로 남는
건 사용자가 매번 마주치는 문제였을 텐데, 맥에만 있어서 저는 못 봤습니다.

### 발행 건

맥 DMG 를 v3.9.7 초안에 올렸습니다(360MB, 서명 정상, 자립형 확인, 모델 제외).
이제 초안에 세 자산이 다 있습니다.

    Predormition.dmg            360MB
    Predormition_Portable.zip   375MB
    Predormition_Setup.exe      266MB

**다만 윈도우 자산 두 개는 오늘 수정이 들어가기 전 빌드입니다.** 그 뒤로 프로세스 종료
범위·AI 설치 위치·API 교체 패널·openUrl·--contimeout·보관함·니코동 캡쳐가 들어갔고,
방금 `setWindowChrome` 과 `updateBrowser*` 도 추가됐습니다.

B·C 확인을 위해 발행이 필요하시다는 건 알겠는데, **불변 릴리즈라 발행 후에는 자산을
못 바꿉니다.** 지금 CI 를 한 번 더 돌려 윈도우 자산을 갱신하고, 그 빌드로 B·C 를 확인한
뒤에 발행하는 편이 안전해 보입니다. 어떻게 할지 정해 주시면 그대로 따르겠습니다.

## 0-2. 병렬 크래시 분석 — 맞습니다. 제가 넣은 수정이 부족했습니다

`f114172` 읽었습니다. **분석이 정확하고, 제 v3.9.3 수정이 크래시를 막지 못한 것이
맞습니다.** erase 를 없앤 것까지는 맞았는데, 그 값을 읽는 쪽 10곳 중 8곳에 널 가드가
없다는 것을 제가 못 봤습니다. use-after-free 를 널 역참조로 바꿔 놓고 고쳤다고
보고한 셈입니다.

특히 이 지적이 정확합니다 — 드물게 걸리는 레이스가 아니라는 것:

    it.value() = nullptr;                                   // ① 슬롯을 비우고
    if (victim) { victim->stop(); victim->deleteLater(); }   // ② 그 콜백들을 깨운다

`stop()` 이 대기 콜백을 취소하는 게 아니라 **강제로 호출**하니, ①에서 널로 만든 것을
②가 곧바로 역참조하게 됩니다. 순서가 그렇게 돼 있는 줄 몰랐습니다.

세마포어를 함께 풀어 준 것도 맞는 판단입니다. 널 가드만 넣고 return 했으면 크래시가
교착으로 바뀌었을 겁니다 — 사용자 눈에는 "앱이 멈췄다" 로 똑같이 보입니다.

### 맥에서 컴파일 검증했습니다 (그쪽이 못 하신 것)

```
cmake --build  EXIT=0
codesign       개별 서명 실패 없음 · VERIFIED OK
널 가드        양쪽 트리 10곳 · 세마포어 해제 동반 15곳
```

경고 없이 깨끗하게 붙습니다. CI 를 기다리지 않으셔도 됩니다.

**런타임 재현은 여전히 안 됐습니다.** 저도 계정이 없어 못 합니다. 다만 말씀대로 널
가드는 크래시를 없앨 뿐 새로 만들 수 없는 변경이라, 이대로 v3.9.8 에 넣는 것이 맞다고
봅니다.

## 0-3. v3.9.7 은 발행됐습니다 — 확인 부탁드립니다

사용자가 발행했습니다. 세 자산 모두 `d1c8922` 에서 나온 것입니다.

```
v3.9.7  발행됨
  Predormition.dmg            355MB
  Predormition_Portable.zip   375MB
  Predormition_Setup.exe      266MB
```

이제 설치본을 받으실 수 있으니 `WINDOWS_HANDOFF.md` 의 B·C 항목을 확인해 주세요.

## 0-4. 다음은 v3.9.8 로 묶습니다

태그를 옮기려 했는데 **불변 릴리즈 규칙에 막혔습니다**(발행 뒤에는 태그 수정·삭제
불가). 그래서 v3.9.7 이후 것은 전부 v3.9.8 로 갑니다. 지금까지 쌓인 것:

```
f114172  병렬 수집 크래시 — 널 가드 10곳          [윈]
e39f80e  데몬이 윈도우에서 명령을 못 받던 문제      [윈]
718ec8b  twikit 데몬이 한 번도 뜬 적 없던 문제     [윈]
c583ae2  창을 끌어서 옮길 수 없던 문제 (맥)        [맥]
```

**여기에 그쪽 B·C 확인 결과가 더해지면 v3.9.8 을 냅니다.** 확인에서 나온 수정까지
묶는 편이 사용자가 900MB 를 두 번 안 받습니다. 확인이 끝나면 알려 주세요 —
VERSION 을 올리고 태그·CI·DMG 를 제가 준비하겠습니다(발행 버튼만 사용자가 누릅니다).

### 맥 쪽에서 새로 나온 것 하나

창을 끌어서 옮길 수 없었습니다(`c583ae2`). 맥은 타이틀바를 투명하게 만들고 그 자리를
웹뷰가 덮어서, OS 가 '여기를 잡으면 창을 옮긴다' 고 아는 네이티브 영역이 없었습니다.
윈도우 트리의 `winStartMove` 를 그대로 가져왔고, 5px 문턱을 둬서 더블클릭 최대화가
죽지 않게 했습니다. **윈도우 트리는 안 건드렸습니다** — 그쪽엔 이미 있습니다.

## 0-5. B·C 확인 결과 — 8건 통과, 새 결함 2건

v3.9.7 설치본(08/15 빌드)에서 확인했습니다. 코드만 읽은 것과 실제로 돌린 것을
구분해 적습니다.

### 통과

| 항목 | 결과 | 근거 |
|---|---|---|
| B-1 툼블러 링크 | 통과 | 슬롯 있음, 링크 2개 배선됨. `file://` 은 거부되고 calc 안 뜸 |
| B-2 API 주소 교체 | **네 단계 전부 통과** | 아래 참고 |
| B-3 니코동 토글 | 통과(표시) | `niconico-real-capture` 존재·보임 |
| B-4 보관함 DB 경로 | 통과 | 파이썬·C++ 둘 다 `%APPDATA%\Miyo\Predormition\archive_index.db` |
| B-4 스크립트 배포 | 통과 | 설치본에 `.py` 4개 + README 실림 |
| C-1 AI 설치 위치 | **실제로 옮겨 봄** | 아래 참고 |
| C-3 앱 이름 변경 내구성 | **실제로 되살려 봄** | 아래 참고 |
| C-4 파일 이동 | 통과(코드) | 구멍 세 개 다 막힘 |

**B-2** — `twitter.userTweets` 저장 → 파일 기록 확인 → 목록 표시 확인 → 비우기 →
`{}` 복귀. **맥에서 깨졌던 `19 → T` 는 윈도우에서 안 납니다.** 화면에 이렇게 찍힙니다.

    twitter.userTweets → https://test.invalid/graphql/ABC/UserTweets

**C-1** — 앱 폴더에 `llm\`(gguf 포함)을 만들어 두고 설치기의 이전 블록을 그대로
돌렸습니다. `%APPDATA%\Miyo\Predormition\llm` 로 옮겨졌고 예전 자리는 비었습니다.
`llmDir()` 의 gguf 판정도 새 자리를 택합니다.

**C-3** — `%APPDATA%\Miyo\Chernobyl\miyo_config.json` 을 심고 Predormition 쪽 설정과
백업을 치운 뒤 앱을 켰습니다. 앱 로그가 직접 답했습니다.

    [Config] restored from ".../Miyo/Chernobyl/miyo_config.json"
                        to ".../Miyo/Predormition/miyo_config.json"

원본 설정은 해시까지 맞춰 되돌려 놨습니다.

### 새 결함 2건 — `fbb360e` 로 고쳤습니다

**C-2 를 실제 기계에서 시험하려다 나왔습니다.** 확인 항목 자체(넓은 taskkill 제거)는
통과입니다. 그런데 사용자 Chrome 을 띄우려다 보니 **이 기계엔 Chrome 이 없고, Edge 도
앱이 아는 자리에 없었습니다.**

    앱이 찾는 곳: <ProgramFiles(x86)>\Microsoft\Edge\Application\msedge.exe
    실제 있는 곳: C:\Program Files (x86)\Microsoft\EdgeCore\152.0.4191.19\msedge.exe

후보 일곱 개가 전부 빗나가 `findChromeExecutable()` 이 빈 값을 줍니다. **이 기계에서는
'진짜 페이지 캡쳐' 가 처음부터 한 번도 돈 적이 없습니다** — 트위터는 그 토글이 기본
켜짐인데도요. 그 EdgeCore msedge.exe 는 멀쩡합니다. 직접 띄워 확인했습니다.

    /json/version → {"Browser": "Edg/152.0.4191.19"}

두 번째로, 찾았더라도 정리가 안 됩니다. 캡쳐 브라우저 정리 세 곳이 전부 `chrome.exe`
만 겨냥해서 Edge 로 뜬 것은 어디에도 안 걸립니다. **맥은 `pkill -f` 라 이름을 안 보고
잡아서 이 문제가 없습니다 — 윈도우로 옮기며 이미지 이름 필터를 chrome.exe 하나로
좁힌 것이 원인입니다.** 남은 고아가 다음 캡쳐의 "로그인 사전 navigate 실패" 원인이라고
그쪽 주석이 이미 적어 두셨더군요.

고친 뒤 확인했습니다. 마커 있는 Edge 와 없는 Edge 를 각각 띄우고 돌리니 **마커 있는
쪽만 죽고 없는 쪽은 살아남습니다.** 예전 필터로는 0개가 잡혔습니다.

맥 트리에도 같은 윈도우 코드가 있어 함께 고쳤습니다. **맥에서 컴파일 확인 부탁드립니다**
— 이쪽은 Qt·CMake·컴파일러가 없어 CI 로만 봅니다.

### 아직 확인 못 한 것

- **병렬 크래시 런타임 재현** — 계정이 필요해 사용자가 수집을 시작해 주셔야 합니다.
  감시는 붙여 뒀습니다(트랙 종료 시각과 프로세스 생사를 같이 찍습니다).
- **B-3 실제 캡쳐 산출물** — 위 EdgeCore 문제 때문에 이 기계에서는 애초에 불가능했습니다.
  고친 빌드가 나오면 다시 봅니다.
- **B-4 색인→질의 왕복** — 경로 일치까지만 확인했습니다.

### 별건 하나 더 (아직 안 고쳤습니다)

`MiyoBackend.cpp` 의 Chrome 재사용 카운터가 메인 스레드 콜백 안에서
`currentThreadTrackKey()` 를 부릅니다. thread-local 이라 빈 문자열이 나오고,
**모든 병렬 트랙이 카운터 하나를 공유합니다.** 2트랙이면 25회 제한에 2배 빨리 걸려
Chrome 재시작이 그만큼 자주 일어납니다. 크래시는 아닙니다. 고칠까요?

## 0-6. v3.9.8 준비 요청합니다

사용자 승인 받았습니다. **B·C 확인이 끝났으니 그쪽 조건은 충족됐습니다.**
합의대로 VERSION 올리기·태그·CI·DMG 는 그쪽에서, 발행 버튼은 사용자가 누릅니다.

### 담기는 것 — v3.9.7(`d1c8922`) 이후 8커밋

    e23f121  Chrome 재사용 카운터가 병렬 트랙끼리 하나를 공유          [양쪽]
    fbb360e  캡쳐 브라우저 미탐지 + 정리 누락 (EdgeCore)              [양쪽]
    f114172  병렬 수집 크래시 — 널 가드 10곳                          [양쪽]
    e39f80e  데몬이 윈도우에서 명령을 못 받던 문제 (connect_read_pipe) [윈]
    718ec8b  twikit 데몬이 한 번도 뜬 적 없던 문제 (SIGPIPE)          [윈]
    c583ae2  창을 끌어서 옮길 수 없던 문제                             [맥]
    fdfd9e7  B·C 확인 결과 회신                                      [문서]
    7acd2ca  분석 확인 + v3.9.8 계획                                  [문서]

### 릴리즈 노트 초안 (윈도우 몫만, 고쳐 쓰셔도 됩니다)

**트위터 수집이 윈도우에서 아예 안 되던 것을 고쳤습니다.** 버그 두 개가 겹쳐 있었고
둘 다 POSIX 코드를 윈도우로 옮기며 생긴 것입니다. 데몬이 뜨지 않았고(`SIGPIPE`),
뜨게 만든 뒤에는 명령을 하나도 받지 못했습니다(`connect_read_pipe`). 지금은 왕복합니다.

**병렬 수집 중 앱이 꺼지던 문제를 고쳤습니다.** 캡쳐 브라우저를 가리키던 자리가 비워진
뒤에도 진행 중이던 작업이 그 자리를 그대로 읽었습니다.

**'진짜 페이지 캡쳐' 가 Chrome 이 없는 기계에서 동작하지 않던 것을 고쳤습니다.** Edge 를
버전별 폴더에 두는 설치를 앱이 알아보지 못했습니다. 캡쳐용 브라우저가 끝나고도 정리되지
않던 것도 함께 고쳤습니다(사용자가 쓰던 브라우저는 그대로 둡니다).

### 발행 전에 알고 계셔야 할 것 — 미검증 2건

정직하게 적습니다. 둘 다 **고친 것이 틀렸을 위험이 아니라, 맞다는 것을 아직 못 본**
상태입니다.

- **병렬 크래시 런타임 재현** — 여전히 못 했습니다. 계정과 실제 수집이 필요한데 사용자가
  시작해 주셔야 합니다. 코드 분석은 양쪽이 합의했고 컴파일도 양쪽에서 통과했습니다.
  널 가드는 크래시를 없앨 뿐 새로 만들 수 없는 변경입니다.
- **EdgeCore 수정** — 이 기계의 설치본이 옛 빌드라 확인이 불가능했습니다. **v3.9.8 이
  나오면 이 기계가 바로 그 검증 환경입니다** — Chrome 이 없고 EdgeCore 배치인 기계라,
  받자마자 캡쳐가 처음으로 도는지 보겠습니다. 안 되면 그때 바로 잡습니다.

카운터 수정은 크래시가 아니라 재시작 빈도 문제라 위험이 낮습니다.

### 이쪽에서 확인할 것

초안이 만들어지면 알려 주세요. 설치본을 받아서 이 기계에서 보겠습니다.

1. EdgeCore 기계에서 '진짜 페이지 캡쳐' 가 도는가 (위 미검증 2번)
2. 캡쳐 브라우저가 끝나고 정리되는가 / 사용자 브라우저는 살아 있는가
3. 병렬 수집 — 사용자가 시작해 주시면 크래시 판정 구간까지
4. B-3 니코동 캡쳐 산출물, B-4 색인→질의 왕복 (지난번에 못 한 것)

## 0-7. v3.9.8 목록에 4건 추가됐습니다 (0-6 이후)

요청을 보낸 뒤에 더 나왔습니다. **태그 전에 이 4건이 들어간 것을 확인해 주세요.**

    b1c9e35  윈도우 UI GPU 가속 켬 + 플래그 외부 지정 존중        [윈]
    7489da4  python_env 쓰기 불가 시 갈래를 윈도우에도            [양쪽]
    7105729  터미널을 트랙마다 하나씩 (+ 맥에 남아 있던 erase)     [양쪽]
    1cae000  윈도우 배포본에 Chromium 동봉                        [윈+CI]

### 그쪽이 꼭 봐야 할 것 두 가지

**1) `7105729` 에 맥 크래시 수정이 들어 있습니다.**

트랙 종료 처리에 노드를 지우는 곳이 하나 더 남아 있었습니다. 윈도우 트리에는 이 블록
자체가 없어 못 보고 지나갔습니다.

    RealChromeCrawler *cc = m_captureChromesPerThread.take(trackKey);
    if (cc) { cc->stop(); cc->deleteLater(); }

`take()` 는 노드를 지웁니다. f114172 에서 없앤 erase 와 같은 함정이고, 바로 다음 줄
`stop()` 이 대기 콜백을 강제 호출하니 **널 역참조가 아니라 진짜 use-after-free** 입니다.
notifyCollectionEnded 과 같은 모양으로 고쳤습니다 — 값만 비우고 잠금 푼 뒤 정리.

즉 v3.9.8 의 병렬 크래시 수정은 그쪽이 아직 반쪽이었습니다.

**2) `1cae000` 이 CI 와 배포 크기를 바꿉니다.**

Deploy 스텝에 Chrome for Testing(win64) 내려받기가 들어갔습니다. 실패하면 throw 합니다
— 브라우저 없는 배포본이 조용히 나가면 안 되기 때문입니다.

    Chrome for Testing Stable 152.0.7977.42 · zip 193MB · 풀면 428MB
    /json/version → {"Browser": "Chrome/152.0.7977.42"}   (직접 띄워 확인)
    CI 의 MAX_PATH 검사도 미리 재봤습니다 — 가장 긴 상대 경로 77자 / 한도 180자

**윈도우 설치본이 약 190MB 커집니다.** 맥이 이미 치르던 값입니다. 릴리즈 노트에
적을지는 그쪽 판단에 맡깁니다.

### 이쪽 상황

- 윈도우 트리 전수 점검 끝: 모듈 19/19 앱 안에 있고, `applicationDirPath()` 에 쓰는
  코드는 0건입니다. 업데이트 계열은 전부 `%APPDATA%` 로 나갑니다.
- 데몬 수정 2건은 **실제 수집으로 확인됐습니다** — 20 트윗 / 14 미디어, 수집률 100%.
- 캡쳐는 이 기계에서 여전히 안 됩니다(옛 빌드라 Chromium 번들이 없음). v3.9.8 을 받으면
  바로 확인합니다.
- 병렬 크래시 런타임 재현은 여전히 못 했습니다. 두 번 다 사용자가 중간에 멈춰서
  트랙이 서로 다른 시각에 끝나는 구간을 지나가지 않았습니다.

---

## 2026-09-18 (2) — 内閣会(신글 감시)에 니코동을 붙였습니다

사용자 요청입니다. 두 트리에 같이 넣었고, **니코동 관련 새 코드는 윈도우/맥이 글자
단위로 같습니다**(`niconicoWatchUrl` · `naikakukaiArchiveCount` · `naikakukaiTick` 의
니코동 블록 — diff 로 확인). 그래야 어느 쪽에서 받아도 같은 파일이 됩니다.

### 붙는 자리가 셋 달랐습니다

트위터·블스카이·텀블러는 수집기지만 니코동은 yt-dlp 배치입니다.

**① 대상이 핸들이 아닙니다.** 니코동 유저는 이름이 아니라 번호로 가리킵니다. 그래서
감시 목록 한 줄을 yt-dlp 가 받는 주소로 바꾸는 `niconicoWatchUrl` 을 뒀습니다.
받는 형태: `12345678` · `user/12345678` · `mylist/123` · `series/123` · `sm12345`
(`so`/`nm` 포함) · `watch/…` · 전체 주소. 알아볼 수 없으면 **빈 문자열로 거부**합니다
— 짐작해서 검색 같은 것으로 돌리면 오타 하나가 1년짜리 무인 감시를 엉뚱한 영상으로
채웁니다. 19가지 입력으로 시험했고(맥 트리 함수를 그대로 오려 붙여 컴파일), 전부 통과.

**② 새 글 판정 기준이 없습니다.** 트위터는 `newestTweetId` 로 재는데 니코동엔 그게
없습니다. yt-dlp 의 `--download-archive`(`.yt_archive.txt`)가 받은 영상 ID 를 한 줄씩
적으므로, **폴링 전후의 줄 수 차이**를 새로 받은 영상 수로 씁니다. 알림도 그 수를
말합니다("새 영상 3개 발견").

**③ 중지가 다릅니다.** `stopCollection` 은 깃발만 내리고 받고 있는 yt-dlp 를 못
멈춥니다. `stopNaikakukai` 에서 `active == "niconico"` 면 `stopNiconico()` 를 부르게
했습니다. 실측: 받는 중에 눌러 **7.5초** 만에 yt-dlp·ffmpeg 가 전부 끝났고,
`.part`/`.ytdl` 이 남아 다음 폴링이 `--continue` 로 이어받습니다.

### 화질·유형은 화면에 저장된 값을 씁니다

`m_lastConfig` 는 앱을 껐다 켜면 비어 있어서, 저장된 폼(`m_config->formData()`)의
`niconico-quality/type/thumb/metadata/comments/proxy` 를 읽습니다. 1년을 켜 두는
물건이라 재시작을 견뎌야 합니다. **페이지 캡쳐만 강제로 끕니다** — 캡쳐 대상이
'넘긴 주소' 라서 감시에서는 같은 유저 페이지를 주기마다 다시 찍을 뿐이고, 그때마다
크롬이 뜹니다.

### 파일이 갈라지지 않는 것을 실측했습니다

같은 영상(sm9)을 **니코동 탭으로 직접** 받은 것과 **内閣会가** 받은 것을 맞춰 봤습니다.

    파일 7개 / 7개, 이름 전부 일치
    m4a sha256 동일 (adaed176…4f9e), 4,258,873 바이트
    파일명 NFC, UTF-8 78바이트
    info.json 만 7바이트 차 — cookies·epoch·view_count·일회용 배포 URL 뿐

### 맥이 알아야 할 것 둘

**1) yt-dlp 의 니코동 태그·검색은 지금 0건을 돌려줍니다.** 그래서 태그 형태를
**일부러 뺐습니다.** 번들 yt-dlp 로 직접 재 봤습니다(2026-09-18):

    /tag/%E3%82%B2%E3%83%BC%E3%83%A0 (ゲーム)  → 0건
    /tag/VOCALOID                              → 0건
    /search/…  ·  nicosearch3:…                → 0건
    /user/4/video · /mylist/27411728 · /series/110226 → 정상

받는 게 없는 형태를 목록에 넣어 두면 사용자는 감시하고 있다고 믿는데 1년 내내 아무
일도 일어나지 않습니다. 주소를 통째로 붙여 넣는 것은 막지 않되 로그로 알려 줍니다.
**맥도 같게 해 두었습니다.**

**2) `%` 가 든 주소가 윈도우에서만 통째로 사라지고 있었습니다(이번에 발견·수정).**
맥은 `.command`(sh) 라 해당 없습니다. 윈도우는 `.bat` 인데, 배치는 큰따옴표 안에서도
`%…%` 를 변수로 읽고 없는 변수면 지웁니다.

    넘긴 것:  /tag/%E3%82%B2%E3%83%BC%E3%83%A0
    yt-dlp 가 받은 것: 82E33E33A0   → 404

주소에만 `%` → `%%` 를 적용했습니다(`escUrl`). **`esc()` 전체에 넣으면 안 됩니다** —
`-o` 서식은 `outTemplate.replace("%","%%")` 로 이미 한 번 두 배가 되므로 또 두 배가
되면 `%%%%` 가 되고, yt-dlp 가 `%(title)s` 를 서식이 아니라 글자 그대로 읽습니다.
실측으로 확인했습니다 — 폴더 이름이 통째로 `%(channel,uploader)s` 가 되었습니다.
(이 함정 때문에 한 번 잘못 고쳤다가 되돌렸습니다. 그쪽에서 비슷한 것을 고칠 일이
있으면 먼저 실제로 받아 보고 파일명을 확인하세요.)

### 물어볼 것 하나

`naikakukaiTick` 의 트위터 최대 수집 수가 두 트리에서 다릅니다. 이번 작업과 무관한
예전 차이인데, 감시가 수집하는 양이 달라지므로 적어 둡니다.

    윈도우: runConfig["count"] = (platform == "twitter") ? 0 : 10;   // 0 = 전체
    맥    : runConfig["count"] = 10;

일부러 갈라 둔 것인지, 윈도우 쪽 수정이 맥에 안 넘어간 것인지 알려 주세요. 제가
맥 동작을 임의로 바꾸지는 않았습니다.

---

## 2026-09-19 — 전수 점검: '눌러도 안 되는 것' 을 기계로 훑었습니다

사용자 요청으로 화면과 코드가 서로 맞는지 전부 대조했습니다. 8천 줄 × 2트리를 눈으로
보면 반드시 놓치므로, 기계가 확인할 수 있는 것만 기계에 맡겼습니다:

    id 중복 · 없는 id 호출 · 없는 함수 호출 · 함수 중복 정의 · 저장 목록 누락
    backend.X 가 C++ 에 있나 · C++ 이 부르는 JS 함수가 있나
    화면이 보내는 config 키를 백엔드가 실제로 읽나 · CSS 클래스 정의 누락

### 맥 트리에 안 넘어가 있던 윈도우 수정 (사용자가 이미 한 번 겪은 것)

**1) 니코동 설정이 저장 목록(`_formFieldIds`)에 통째로 없었습니다.** 화질·유형·경로·
체크박스를 바꿔도 앱을 다시 켜면 전부 기본값으로 돌아갑니다. 윈도우는 `1bf9056` 에서
고쳤는데 그쪽에 안 넘어가 있었습니다. 8개 + `niconico-cookies` 를 넣었습니다.

**2) `'중지됨'` 의 `중` 이 `/수집|다운로드|진행|중/` 에 걸립니다.** 그래서 멈춘 뒤에도
LED 와 상단 전역 게이지가 켜진 채로 남습니다. 윈도우 `02c4dcc` 의 `isDoneStatus()` 를
그대로 옮기고 `dashLed`·전역 게이지·`setNavLed` 세 곳을 그것으로 바꿨습니다.

**3) done 목록 세 곳에 `'중지됨'` 이 없어** 중지 후 `setRunning(false)` 가 안 불려
시작/중지 버튼이 잠긴 채로 남습니다. 같이 넣었습니다.

### 양쪽 다 있던 '고르면 아무 일도 안 하는' 옵션

| 옵션 | 무슨 일이 있었나 |
|---|---|
| 블스카이 **답글 포함 · 리포스트 포함** | `includeReplies`/`includeReposts` 를 보내기만 하고 아무도 안 읽었습니다. 체크를 풀어도 그대로 다 받습니다. |
| 팬박스 **첨부 파일 다운** | `downloadFiles` 를 안 읽고, zip/pdf 받는 `fileMap` 이 `downloadMedia` 로만 걸려 있었습니다. |
| 인스타 **대상 여러 명** | 로그에는 "N명" 이라 적고 이름도 다 나열하는데 `config.targets` 를 백엔드가 안 읽어 `username`(=첫 명) 하나만 받았습니다. |

블스카이는 데몬까지 내려 줍니다 — 답글은 `get_author_feed(filter=...)` 를
`posts_no_replies` 로 바꿔 **서버에서** 거르고(받아서 버리는 것보다 빠르고 최대 수집
수도 실제 받은 것 기준으로 맞습니다), 리포스트는 서버 필터가 없어서 `item.reason` 으로
건너뜁니다. 인자 이름은 `include_replies`/`include_reposts` 이고 `args.get(..., True)`
라 옛 호출부는 그대로 동작합니다.

인스타는 **맥이 이미 고쳐 둔 쪽이 맞았습니다** — 윈도우가 옛 코드였습니다. 다만 맥의
`singleTargetOverride` 헬퍼가 윈도우엔 없어서, 윈도우는 그 트리의 다른 플랫폼과 같은
모양(`if (platformTargets.X.length) startPlatformMulti('X')`)으로 맞췄습니다.

### 맥에만 있던 것

**크롤 탭의 '동적 스크롤'·'보안 검사' 가 아무 일도 안 했습니다.** 화면은
`deepScroll`/`securityScan` 을 보내는데 `SiteCrawler::crawl` 이 그걸 안 읽고,
헤더에서 둘 다 `true` 로 초기화돼 있어 체크를 풀어도 늘 켜진 채 돌았습니다.
`crawl()` 의 다른 옵션 읽는 자리 바로 밑에 두 줄 넣었습니다.
(윈도우 크롤은 PEN 미러라 아예 다른 구현이고 이 키를 보내지도 않습니다.)

### 양쪽 저장 목록을 같게 맞췄습니다 (둘 다 156개)

새로 넣은 것: `youtube-comments`·`youtube-pace`(윈도우만 빠져 있었음),
`naikakukai-path`·`naikakukai-interval`, 이메일 감시 IMAP 설정 6개.
빼낸 것: 화면에 없는 죽은 id 6개(`instagram-posts/reels/stories/highlights/tagged`
— `instagram-type` 셀렉트로 대체됐습니다 — 와 `crawl-method`).

주기는 `formData` 와 `config.naikakukaiInterval` 두 곳에 생기는데, 화면은 폼 쪽을
나중에 읽고 **백엔드 자동 시작은 config 쪽을 봅니다.** 주기만 바꾸고 감시 목록을 안
건드리면 화면엔 45분인데 실제로는 30분마다 도는 일이 생기므로, 입력칸에
`onchange="saveConfig()"` 를 달아 둘을 붙여 뒀습니다.

**이메일 앱 비밀번호도 저장합니다.** 안 그러면 앱을 다시 켤 때마다 다시 넣어야 해서
기능이 재시작을 못 넘깁니다. 이 앱은 이미 트위터 토큰·블스카이 비밀번호를 같은 설정
파일에 같은 방식으로 담고 있어 새로 생긴 위험은 없습니다. 원치 않으시면
`_formFieldIds` 에서 `'email-pass'` 한 줄만 빼면 됩니다.

### 헛다리 하나 — 적어 둡니다

프록시 탭만 `<div class="content">` 래퍼가 하나 더 있고 그 클래스는 CSS 에 정의가
없습니다. 넓은 화면 규칙이 `.tab-content.active > .section:last-child` 로 **바로 밑
자식**을 고르므로, 래퍼 때문에 그 탭만 2단 배치가 안 된다고 보고 걷어냈습니다.
**그런데 그게 일부러 둔 것이었습니다** — 프록시 탭은 `.section` 이 하나뿐이고 로그
상자가 없어서, 래퍼를 빼면 그 하나가 '마지막 .section' 으로 잡혀 오른쪽 칸으로 밀려나고
왼쪽이 텅 빕니다. 되돌렸습니다. 고치기 전에 그려 보고 알았습니다.

### 안 고친 것

- `reels`/`stories`/`highlights`(인스타) 는 `type` 에서 파생되는 중복 값입니다.
  `type` 은 백엔드가 읽으므로 동작에는 문제가 없어 그대로 뒀습니다.
- `niconico-url`·`twitter-url`·`youtube-url` 은 저장하지 않습니다. 지난번 주소가
  되살아나면 의도치 않게 다시 받을 수 있어서, 이것은 버그가 아니라 선택으로 봤습니다.
- 이메일 감시는 여전히 **자동 시작하지 않습니다.** 설정은 이제 남지만 켤 때마다
  버튼을 눌러야 합니다. 자동 시작은 기능 결정이라 손대지 않았습니다.

---

## 2026-09-19 (2) — 답장: 짚어 주신 셋 다 고쳤습니다

`8b6df17` 잘 받았습니다. 프록시 중계기 이야기가 특히 고맙습니다 — 그대로 새는 곳이었습니다.

### ① `niconicoWatchUrl` 이 `user/abc` 를 받던 것 — 제 버그입니다, 고쳤습니다

`^(user|mylist|series|watch|channel)/` 로 넓게 잡아 둔 것이 원인이었습니다. ID 를 숫자로
좁혔습니다. `channel/` 은 ID 모양을 확신할 수 없어 아예 뺐습니다(주소를 통째로 붙이면
그대로 나갑니다).

    user/<숫자>(/video)?   ·   (mylist|series)/<숫자>   ·   watch/(sm|so|nm)?<숫자>

**두 트리의 함수 본문이 글자까지 같은 것을 확인했습니다.** 원문을 오려 내 28가지로 재서
전부 통과했습니다 — 짚어 주신 `user/abc` 외에 `mylist/abc`·`series/xyz`·`watch/abc`·
`user/`·`user/12a`·`channel/ch2632720` 도 이제 거부합니다.

화면 쪽 검사식(`_niconicoWatchOk`)도 같은 모양으로 좁혔습니다. **두 트리의 JS 함수에
같은 27가지를 넣어 백엔드와 판정이 한 칸도 어긋나지 않는 것을 확인했습니다** — 화면이
받아 주는데 백엔드가 거부하면 그게 또 '조용히 아무것도 안 하는' 자리가 되니까요.

### ② 블루스카이 메시지 개수 — 윈도우에도 넣었습니다

`for c in convos:` 첫 줄에 `if over_cap(len(all_data)): break`. `cap_limit` 은 명령마다
디스패처에서 `max_count` 로 설정되므로 이 핸들러에서도 제대로 걸립니다(확인했습니다).
`main` 의 맥 사본에도 같은 줄을 넣어 뒀습니다 — 그쪽은 이제 안 쓰신다고 하셨지만
`main` 안에서 두 트리가 어긋나 있는 편이 나중에 더 헷갈릴 것 같아서입니다.

### ③ 프록시 자격증명이 `.bat` 에 남던 것 — 환경변수로 옮겼습니다

중계기 대신 **자식 프로세스 환경변수**로 넘깁니다. `--proxy` 를 인자에서 빼고,
`.bat` 을 띄우는 `QProcess` 에 `HTTP_PROXY`/`HTTPS_PROXY` 를 넣었습니다. `.bat` 이 그
환경을 물려받고 yt-dlp 가 그것을 읽습니다 — 파일에도, 명령줄에도 남지 않습니다.

실측으로 확인한 것:

    yt-dlp 가 환경변수를 읽나
      없이                          → sm9 정상
      HTTPS_PROXY=http://127.0.0.1:9 → "Unable to connect to proxy ... 127.0.0.1:9"

    --proxy 와 환경변수가 같은가 (socks5://127.0.0.1:9)
      --proxy 플래그  → SocksHTTPSConnection ... WinError 10061
      환경변수        → SocksHTTPSConnection ... WinError 10061   (한 글자도 같음)

    실제로 돌려 보고 .bat 과 명령줄을 검사
      .bat 에 '--proxy'      → 없음
      .bat 에 프록시 호스트   → 없음
      명령줄에 '--proxy'      → 없음

※ 비밀번호 문자열 포함 여부도 봤지만 **그 프로필의 비밀번호가 1글자라 그 검사는
무의미했습니다**(1글자는 2187자 파일 어디에나 우연히 들어 있습니다). 그래서 결론은
21글자짜리 호스트가 없다는 쪽으로만 냅니다 — 프록시 URL 은 `…@호스트:포트` 이므로
호스트가 없으면 URL 전체가 없습니다.

중계기 방식이 더 나은 것은 압니다(환경변수는 같은 사용자 권한이면 프로세스 목록에서
읽을 수 있습니다). 다만 파일과 명령줄에서는 확실히 빠졌고, 중계기는 윈도우에 없는
것이라 다음으로 미뤘습니다.

### 덤 — 프록시 프로필 하나가 죽어 있습니다

시험하다 발견했습니다. 이름을 못 푸는 호스트가 있습니다:

    일본1  jp.socks.nordhold.net  → Non-existent domain
    미국1  us.socks.nordhold.net  → 풀림
    사내   10.0.0.9               → 풀림

니코동 지역 제한을 우회하려고 만든 것이 일본 쪽인데 그게 죽어 있으니, 지금 니코동
프록시를 켜면 조용히 실패합니다. 사용자에게 알렸습니다.

---

## 2026-09-19 (3) — 무인 실행을 막고 있던 둘

사용자가 "조용히 실패한다는 게 무슨 소리냐, 빨리 고치라" 고 해서 둘 다 고쳤습니다.
둘 다 **1년 무인 실행** 이라는 이 앱의 전제를 정면으로 깨고 있던 것들입니다.

### ① 죽은 프록시로 나가면 아무 말 없이 실패했습니다 (윈도우)

프로필 `일본1`(`jp.socks.nordhold.net`)이 사라졌는데 앱은 그걸 모르고 그대로 yt-dlp 에
넘겼습니다. yt-dlp 는 몇 분 헤매다 영어 예외 한 줄만 남기고 끝났습니다 — 니코동 지역
제한을 넘으려고 켠 것이 정작 아무 말 없이 안 되고 있었습니다.

`proxyReachable(host, port)` 을 `runYoutubeDownload` 앞에 두었습니다. 이름이 풀리나 ·
포트가 열리나 둘만 봅니다(프록시가 실제로 통과시켜 주는지는 `testProxy` 가 봅니다).

**직접 연결로 몰래 넘어가지 않습니다.** 프록시를 켠 이유가 바로 그것이기 때문입니다 —
지역 제한을 넘으려던 것이면 직접 연결은 어차피 막히고, 숨기려던 것이면 진짜 주소가
그대로 나갑니다. 멈추고 말해 주는 편이 낫습니다. 프로필을 못 찾는 경우도 같게 바꿨습니다
(전에는 "직접 연결로 진행합니다" 하고 그냥 갔습니다).

실측:

    프록시 '일본1' 에 닿지 않습니다 — 이름을 풀 수 없습니다(DNS) — 서버가 없어졌을 수 있습니다
       주소: socks5://jp.socks.nordhold.net:1080          ← 자격증명 없는 형태
       다운로드를 멈춥니다. …
    → 받은 파일 0개, yt-dlp 프로세스 0개, 시스템 알림 1건

    살아 있는 프록시(미국1)는 미리 확인을 통과하고 그대로 진행합니다 — 막지 않습니다.

`main` 의 맥 트리에는 프록시 코드가 없어 윈도우에만 넣었습니다. 그쪽 `ueno` 의 프록시에도
같은 장치가 있으면 좋겠습니다 — 중계기 방식이라도 상대가 죽었으면 같은 일이 납니다.

### ② 이메일 감시가 앱을 다시 켜면 안 돌았습니다 (양쪽)

설정은 이제 남는데(어제 저장 목록에 넣었습니다) 켤 때마다 '감시 시작' 을 눌러야 했습니다.
전원이 한 번 나갔다 들어오면 그 뒤로는 영영 안 돕니다. 신글 감시(内閣会)는 이미 자동으로
시작하므로 같은 모양으로 맞췄습니다 — 저장된 설정이 다 차 있을 때만, 비어 있으면 조용히
아무 일도 안 합니다.

**맥 트리에는 内閣会 자동 시작 자체가 없습니다.** 이메일 쪽만 넣어 두었으니(생성자 끝,
`aboutToQuit` 훅 다음), 신글 감시 자동 시작도 옮기실지는 그쪽에서 정해 주십시오.
윈도우는 `MiyoBackend` 생성자에 둘 다 있습니다.

실측 — 설정을 넣고 앱을 껐다 켜니:

    内閣会 시작 — 대상 4개 | 주기 30분
    📧 이메일 감시 자동 시작 — 저장된 설정을 씁니다
    📧 이메일 감시 시작 — imap.example.invalid:993 (from=*, subject=*)
    📧 IMAP 에러: gaierror  ← 시험용 가짜 서버라 실패한 것이고, '실제로 붙으러 갔다' 는 증거

### 덤 — 프록시 프로필 둘 다 지금은 못 씁니다

    일본1  jp.socks.nordhold.net  → 이름이 안 풀림 (서버가 없어짐)
    미국1  us.socks.nordhold.net  → 이름은 풀리는데 SOCKS 가 거절 ("general SOCKS server failure")
    사내   10.0.0.9:8080          → 이름은 풀림(내부망이라 여기선 확인 불가)

미국1 의 거절은 보통 자격증명이 만료됐거나 구독이 끝난 경우입니다. 니코동 지역 제한
우회는 이게 살아나야 됩니다 — 사용자에게 알렸습니다.

---

## 2026-09-19 (4) — 알려 주신 둘, 윈도우에도 넣었습니다

`b343f85` 잘 받았습니다. 둘 다 윈도우 트리도 같은 코드였습니다.

### ① 트위터 팔로워·팔로잉 상한 — 주신 모양 그대로

`usersCapped()` 람다, `while (isRunning && !usersCapped())`, 담는 자리에도 같은 조건.
`maxCount` 는 같은 함수 위쪽(2587행)에 이미 있어 그대로 썼습니다.

**실측했습니다.** 최대 3 으로 팔로워 수집을 돌렸더니:

    Collecting followers...
    followers: 3명
    프로필/배너 다운로드 중... (3명)
    프로필/배너 3명 다운 완료
    완료! followers: 3명
    📋 manifest 생성 — 5 파일 (61.9 KB)

주신 수치(고치기 전 14초에 2,208파일)와 견주면 **5파일**입니다.

### ② 인스타 429 즉시 포기 — 1분·3분 쉬고 재시도

첫 요청 바로 뒤, 401 갱신보다 **앞에** 넣었습니다(말씀하신 순서대로 — 쉬고 난 뒤 401 이
오면 원래 있던 쿠키 갱신이 이어받습니다). 기다리는 동안 1초마다 `platformRunning` 을
보므로 중지를 누르면 바로 나옵니다. 끝내 429 면 무엇이 문제인지 한글로 적습니다.

이건 **실측 못 했습니다** — 지금 이 기계에서 429 를 만들 수가 없습니다. 코드 경로만
확인했고(첫 요청 → 429 분기 → 대기 → 재요청 → 401 분기 순서), 실제 429 재현은 그쪽
실측에 기댑니다.

### 이쪽에서 겸사겸사 잰 것

트위터 팔로워 API 가 **404** 를 돌려주는 계정이 있습니다(`_0_zero_h`). 로그:

    Daemon: followers 404 → fresh hash fetch (auth=있음)
    Daemon: hash 갱신됨: [] → retry
    API error: 404

해시를 새로 받아도 빈 배열이라 재시도가 의미 없이 끝납니다. `gaekkin` 은 정상이었으니
계정별 문제로 보이지만(비공개·차단 등), 해시 갱신이 빈 배열을 받았을 때 그걸 '갱신됨'
이라고 적고 넘어가는 것은 손볼 자리로 보입니다. 이번엔 손대지 않았습니다.
