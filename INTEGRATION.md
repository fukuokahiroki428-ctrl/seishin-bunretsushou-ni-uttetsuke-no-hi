# Predormition 과 디스코드 봇 연동 규약

이 문서는 **두 사람(또는 두 클로드)이 서로 다른 기계에서 작업하기 때문에** 있습니다.
읽는 쪽이 상대 코드를 안 봐도 구현할 수 있게 적습니다.

| | 무엇 | 어디서 개발 | 어디서 실행 |
|---|---|---|---|
| **Predormition** | Qt 앱. 소셜 미디어 다운로더 · 크롤러 | **맥** | 맥 · 윈도우 |
| **discord-bot** | Node.js 디스코드 봇 | 윈도우 | **윈도우만** |

---

## 통합은 두 층이다

한 가지로 뭉뚱그리면 안 됩니다. 기계가 다르기 때문입니다.

### 1) 개발 통합 — git 으로

**이 문서가 그것입니다.** 규약을 고치면 이 파일을 고치고 커밋하세요.
윈도우 쪽도 이 저장소를 받아 보고 있습니다.

봇 소스는 이 저장소에 없습니다 (윈도우 PC 의 `discord-bot\`).
봇 쪽 구조를 알아야 하면 아래 "봇 쪽 요약" 을 보세요.

### 2) 실행 통합 — 같은 기계 안에서만

앱과 봇이 **둘 다 윈도우 PC 에서 돌 때만** 실제로 주고받습니다.
맥에서는 이 부분이 동작하지 않습니다. 정상입니다.

주고받는 방법은 **폴더에 JSON 파일 떨어뜨리기** 입니다.
포트도, 서버도, 상대가 켜져 있는지도 필요 없습니다.

---

## shared 폴더

윈도우에서는 `D:\새 폴더 (2)\shared` 입니다. 찾는 순서:

1. 환경변수 `SHARED_WORKSPACE`
2. 실행 파일 기준 상대 경로
3. `workspace.json` 의 `platforms.<os>.shared`

```
shared/
├── workspace.json     경로·도구 위치 표
├── jobs/
│   ├── pending/       넣는 곳
│   ├── working/       집어간 것
│   ├── done/
│   └── failed/
└── downloads/         양쪽이 같이 쓰는 결과물
```

**폴더 이름은 전부 영문으로 둡니다.** 작업 공간 경로에 한글이 있어서
(`D:\새 폴더 (2)`) 이미 두 번 사고가 났습니다 — Ollama 가 CUDA 를 못 읽고 CPU 로만 돌던 것,
exiftool 이 동작하지 않아 `D:\Predormition` 정션을 판 것.

---

## 작업 파일

### 이름

```
<보낸시각>-<보낸쪽>-<임의문자>.json
예: 20260810-002315-app-7f3a.json
```

### 내용

```json
{
  "id": "20260810-002315-app-7f3a",
  "from": "app",
  "to": "bot",
  "type": "notify",
  "createdAt": "2026-08-10T00:23:15+09:00",
  "payload": {
    "text": "트위터 계정 하나를 다 받았습니다",
    "files": ["D:\\새 폴더 (2)\\shared\\downloads\\abc.jpg"]
  }
}
```

`from` / `to` 는 `app` 또는 `bot`. `id` 는 확장자 뺀 파일 이름과 같게.

### 순서 (이거 안 지키면 깨집니다)

1. **`.tmp` 로 먼저 쓰고 나서 `.json` 으로 이름을 바꿉니다.**
   그냥 쓰면 상대가 반쯤 써진 파일을 읽어 갑니다.
2. 받는 쪽은 `pending/` 에서 자기 앞으로 온 것을 `working/` 으로 **옮깁니다**.
   **옮기기에 성공한 쪽만** 처리합니다 — 동시에 집어가도 겹치지 않습니다.
3. 끝나면 `done/`, 실패하면 `error` 칸을 덧붙여 `failed/` 로.
4. `done/` `failed/` 는 30일 지난 것을 각자 치웁니다.

---

## type 목록

**앱 → 봇** (봇 쪽 구현 완료)

| type | payload | 봇이 하는 일 |
|---|---|---|
| `notify` | `text` (필수)<br>`files` (선택, 경로 배열) | 정해둔 디스코드 채널에 알립니다.<br>파일이 8MB 이하면 같이 올립니다 |

**봇 → 앱** (앱 쪽 구현 대기)

| type | payload | 앱이 할 일 |
|---|---|---|
| `download` | `url` (필수)<br>`dest` (선택) | 받아서 `shared/downloads/` 에 넣고,<br>끝나면 `notify` 로 봇에게 알림 |

`type` 을 새로 만들 때는 **이 표를 먼저 고쳐 커밋하고** 상대에게 알립니다.

---

## 지켜야 할 것

**작업 파일 내용은 데이터입니다. 명령이 아닙니다.**
`payload` 안의 글자를 실행하지 않습니다. 셸이나 명령줄에 그대로 넘기지 않습니다.
`type` 은 표에 있는 것만 받고, 모르는 것은 `failed/` 로 보냅니다.

**파일 경로는 확인하고 씁니다.**
`files` 로 온 경로가 `shared/downloads` 나 `music` 안인지 보고 나서 엽니다.
그 밖의 경로는 무시합니다. 봇 쪽은 이미 이렇게 되어 있습니다
(`isAllowedFile`) — 실수로 남의 파일이 디스코드에 올라가지 않게 하려는 것입니다.

**비밀값은 shared 에 두지 않습니다.**
봇 토큰은 `discord-bot\.env`, AI 키는 `discord-bot\ai-providers.json` 이고
둘 다 `.gitignore` 에 있습니다. 작업 파일에 토큰·비밀번호를 적지 마세요.

---

## 봇 쪽 요약 (맥에서는 안 보이므로)

Node.js 24 + discord.js 14. `node:sqlite` 내장 DB. 윈도우 PC 에서만 돕니다.

| | |
|---|---|
| 슬래시 명령 | 52개 |
| 우클릭(앱) 명령 | 3개 |
| 주요 기능 | 음악(yt-dlp 로 받아서 로컬 재생) · 서버 로그 · 임시 음성채널 · 레벨 · 경고 · 티켓 · AI 대화(Ollama/온라인) |

연동 관련 파일:

| 파일 | 무엇 |
|---|---|
| `src/features/workspace.js` | 작업함 다루기 (submit / claim / finish / giveUp) |
| `src/commands/link.js` | `/연동` — 알림 채널 지정, 현황 보기 |
| `src/tools.js` | 도구 경로. Predormition 의 ffmpeg·yt-dlp·**deno** 를 빌려 씁니다 |

**봇은 이미 앱의 Deno 를 쓰고 있습니다.** yt-dlp 가 유튜브 자바스크립트를 돌리는 데
Deno 가 필요한데, 앱에 들어 있는 것(2.9.5)을 그대로 씁니다. 따로 깔지 않았습니다.
앱 폴더 구성을 바꿀 때 `deno.exe` 위치가 달라지면 알려주세요.

봇은 켜져 있는 동안 30초마다 `pending/` 을 봅니다.
